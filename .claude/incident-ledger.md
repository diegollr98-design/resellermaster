# Ledger de Incidentes — RESELLERMASTER

**Append-only.** Registro FACTUAL de incidentes/near-misses cazados por el Loop de Cambios (`.claude/rules/change-loop.md` §F) y por el Loop de Verdad (`rules/truth-loop.md`). Vive en `.claude/` → **NO se auto-carga** (sin coste de tokens por sesión). **No se editan entradas pasadas; solo se añaden** (más reciente arriba).

**Para qué:** materia prima de la auto-mejora. **Solo `/optimize` (Paso 2)** promueve una entrada a regla, y solo con **≥2 incidencias independientes** de la misma clase, **o 1 de clase irreversible** (ficha publicada mal, precio inventado, foto cruzada, coste desbocado, pérdida de un lote). n=1 no irreversible → se queda aquí, no toca reglas. Al promover, la regla nueva se **etiqueta con el/los id** de esta lista; `/optimize` (Paso 5) retira las reglas cuyos ids no reaparecen en >5 sesiones.

**Formato:** `- [id] fecha · clase · qué pasó · evidencia (comando/veredicto) · estado`

**Clases:**
- `alucinacion` — el pipeline afirmó un atributo (marca/talla/material/medida) que no era legible en la foto.
- `precio` — precio dado sin comparables, o con comparables que no eran del mismo producto.
- `agrupacion` — foto asignada al producto equivocado, o producto partido en dos fichas.
- `coste` — gasto de API no estimado, no mostrado, o disparado respecto a lo previsto.
- `persistencia` — trabajo de curado perdido (crash, rerun de Streamlit, estado sólo en memoria).
- `verificacion` — un agente reportó sin verificar (dijo "OK" sin ejecutar/mirar la foto).
- `plataforma` — un campo generado no encaja con lo que Wallapop/Vinted acepta realmente.
- `proceso` — fallo de método (saltarse un gate, no pedir confirmación, encadenar fases sin OK).

**Estado:** `pendiente (≥2)` = registrado, aún no promovido · `promovido → <regla>` = ya es regla dura.

**Nota:** la existencia de este fichero es lo que **da de alta a este proyecto** en el detector global `C:\Users\diego\.claude\hooks\optimize-nudge.sh` (opt-in por presencia del ledger). El hook avisará en `SessionStart` si hay incidentes pendientes, si los ficheros auto-cargados pasan de 3000 líneas, o si han pasado >21 días desde el último `/optimize`.

---

<!-- entradas nuevas debajo, más reciente arriba -->

- [INC-028] 2026-07-21 · verificacion · **Renombré una constante con `sed` en un fichero y dejé la referencia vieja en OTRO; ni ruff, ni el import, ni el arranque headless lo vieron -- lo cazó DIEGO pulsando el botón.** Hice pública `pricing._NOTA_PRECIO_PEDIDO` → `NOTA_PRECIO_PEDIDO` con `sed -i` sobre `core/pricing.py`, pero `ui/export.py:305` seguía leyendo `pricing._NOTA_PRECIO_PEDIDO` → `AttributeError` **al pulsar «Buscar comparables»**. Verifiqué el cambio con: 52 tests verdes, ruff limpio, `python -m compileall`, y arranque headless de Streamlit SIN traceback -- **y aun así el botón petaba**, porque `_render_precio` (línea 305) sólo se ejecuta DESPUÉS del clic, y ningún test lo pulsaba. · **Tercera incidencia de la clase `[INC-006]`/`[INC-021]`** ("la superficie que Diego TOCA CON LAS MANOS necesita su test de `AppTest`; un flujo que cuesta un clic arrancar no está probado hasta que alguien paga ese clic -- y el arranque headless renderiza el árbol inicial pero NO dispara los `on_click`"). El precio se añadió con tests de `AppTest` del render inicial (pasaban) pero NINGUNO que pulsara el botón. Es también `[INC-024]`/§17 en otra forma: un `sed` global es una edición sin `if` que lo verifique -- "renombré esto en todos lados" es una afirmación que se EJECUTA (grep del nombre viejo en TODO el repo, no en un fichero), o no se hace. · **La regla `change-loop.md` §C4 ya está PROMOVIDA** (desde `[INC-006]`) y exige `AppTest` en la UI de curado que mueve fotos -- pero NO cubría "todo botón nuevo de la UI ejecuta su `on_click` en un test". · evidencia: traceback de Diego (`ui/export.py:305`, `AttributeError: module 'core.pricing' has no attribute '_NOTA_PRECIO_PEDIDO'`); `grep -rn _NOTA_PRECIO_PEDIDO ui/ core/` → 1 referencia viva. · **Fix:** la referencia + un `AppTest` que PULSA el botón con un buscador falso (sin red) y ejecuta `_render_precio` entero. · estado: pendiente (≥2, con `[INC-006]`/`[INC-021]`) — regla candidata: **"todo BOTÓN nuevo de la UI lleva un test de `AppTest` que lo PULSA (ejecuta su `on_click`/rerun), no sólo que renderiza la pantalla inicial -- el arranque headless y el render inicial no pagan el clic. Y un rename global (`sed`) se verifica con un `grep` del nombre viejo en TODO el repo, no en el fichero editado."**

- [INC-027] 2026-07-18 · precio · **La MEDIANA de comparables salía de un identificativo DÉBIL (un tipo de prenda genérico) y era el catálogo entero disfrazado de "el precio de tu producto".** El motor de precio v2 (`core/pricing.py::tasar`, Fase 4 Frente 2) exige un identificativo para tasar, pero `CAMPOS_IDENTIFICATIVOS = (marca, modelo, tipo)` contaba `tipo` como suficiente él solo. Reproducido: una prenda con la marca `inferido`/`null` (etiqueta no legible -- el caso EXACTO de ropa genérica de `truth-loop.md` §D.2) pero el título confirmado dice "sudadera" → `tasar` buscaba `"sudadera"` a secas → mediana del catálogo entero de sudaderas (toda marca/talla/estado), presentada con la MISMA UI de confianza que una cohorte estrecha. El único delator era el caption `Búsqueda: «sudadera»`, que es un pie de foto (`decision-making.md` §12), no una defensa. Es el modo de fallo del proyecto en su forma más pura: un número plausible con URLs reales que NO representa el producto. · **La lección: la PRESENCIA de un identificativo no basta; su FUERZA es parte del gate.** `truth-loop.md` §D.2 define la cohorte como "marca + tipo + talla" -- `tipo` solo no es una cohorte. Fix: `tasar` exige `CAMPOS_IDENTIFICATIVOS_FUERTES = (marca, modelo)`; sin ellos → `mediana=None` + motivo "cohorte demasiado amplia". (`buscar()` v1 sí acepta tipo solo: solo arma una URL, Diego mira; un NÚMERO exige más.) · **Dos hallazgos menores del mismo audit, cerrados:** (a) `_parsear` prometía en su docstring "parseo raro → []" pero el `try/except` no envolvía el bucle de items → `items` de forma inesperada (str/None/[None]) lanzaba `AttributeError` en vez de degradar; el contrato se cumple ahora POR CONSTRUCCIÓN, no porque la UI lo atrape aguas abajo (misma clase que `[INC-024]`/§17: una promesa en un docstring no es una garantía hasta que el código la fuerza). (b) `bool` es subclase de `int` → `True` colaba como precio 1,0 € en la mediana; excluido explícitamente. · evidencia: `listing-audit` (2026-07-18, veredicto RIESGO -- no BLOQUEANTE: el invariante caro "número sin ≥5 comparables" resiste), reproducido y re-derivado por el orquestador ejecutando `tasar({'tipo':...})` → mediana None tras el fix. · estado: pendiente (≥2) — regla candidata: **"un comparable/cohorte se valida por la FUERZA de su identificador, no solo por su presencia: un identificativo débil (un tipo genérico, una talla) produce una cohorte = catálogo entero, cuya mediana es un número plausible que no representa el producto. La fuerza del match es parte del gate, no solo que exista."**

- [INC-026] 2026-07-17 · verificacion · **Una tabla de constantes escrita A OJO para "casar" con lo que produce un transformador hacía lo CONTRARIO en el caso más común -- y lo cazó el `listing-audit`, no la suite (94 tests verdes con la verdad rota).** El nuevo `core/categorias.py` (Fase 4, candidatas de hoja de categoría) sesga el ranking por género: una prenda de "hombre" sube las hojas de Hombre. `_GENERO_STEMS` se escribió a mano con los stems `{"hombr","masculin","femenin"}` **asumiendo** lo que `_stem` produciría -- pero `_stem("hombre")=="hombre"` (solo recorta plural s/es), así que `_genero_en(["hombre"])` devolvía `None` y una sudadera de HOMBRE sacaba las 3 candidatas de MUJER. Asimétrico y perverso: `mujer` (singular) SÍ casaba, `hombre` (singular, la forma natural más común) NO → el sesgo de género quedaba muerto justo para la mitad de los casos, y la feature anunciada para desambiguar género hacía lo contrario. · **La clase, y es `[INC-024]` en otra ropa:** una constante que debe COINCIDIR con la salida de una función (`_stem`) no se ESCRIBE a ojo -- se DERIVA aplicando esa función (`{_stem(s) for s in sinonimos}`), o coincide por suerte hasta que deja de hacerlo. Es la misma familia que "un umbral fuzzy se calibra contra el vocabulario real, no a ojo" (`[INC-024]`) y que "un `if` comprueba EXACTAMENTE lo que promete, no un proxy" (`[INC-015]`, §17). · **Segundo hallazgo del mismo audit (SERIO, también arreglado):** las candidatas son una ayuda AUXILIAR pero `construir_payload` llamaba a `categorias.candidatas` con un `raise` duro sin proteger → un snapshot `core/taxonomia/*.json` ausente/corrupto tumbaba el export ENTERO de esa plataforma (título/estado/talla/fotos = el 66% del tiempo), contradiciendo la promesa del módulo ("si ninguna encaja, navegas a mano, nunca peor que hoy") y `decision-making.md` §13. Fix: try/except → candidatas vacías + aviso marcado, el payload sigue. · evidencia: `listing-audit` (2026-07-17, veredicto RIESGO -- no BLOQUEANTE porque Diego elige toda hoja a mano, nada se auto-publica), reproducido y re-derivado por el orquestador ejecutando: `_genero_en(["hombre"])==None`; `candidatas("moda","...hombre...","vinted")` → 3 hojas de Mujer. **Fix:** derivar `_GENERO_STEMS` con `_stem` + tests de género (hombre singular/plural, masculino, femenino) que fallaban antes del fix; try/except en `construir_payload` + test de snapshot ausente. · estado: pendiente (≥2 con `[INC-024]`) — regla candidata: **"una constante/tabla que debe COINCIDIR con la salida de una función (un stem, un normalizador, un hash) se DERIVA aplicando esa función, nunca se escribe a ojo -- si no, coincide por suerte hasta que un caso no cubierto la rompe en silencio; y lo caza el audit adversarial, no la suite del propio autor."**

<!-- ═══ nota /optimize 2026-07-17 — PROMOCIONES (writer único de reglas) ═══
Promovidos a regla dura tras acumular ≥2 de clase o 1 irreversible. Los `[id]` de origen quedan etiquetados en la regla; se retiran de las reglas si el id no reaparece en >5 sesiones.
- [INC-013][INC-015][INC-019][INC-020][INC-024] → decision-making.md §17 (una garantía en prosa no está garantizada hasta que un `if` la fuerza sobre el valor que se publica; afirmación de seguridad en comentario/commit se ejecuta antes de escribirla).
- [INC-023][INC-010][INC-011][INC-012] → decision-making.md §18 (quién decide, no el algoritmo: juicio imposible → transcripción o hueco; cobertura con oráculo; dos señales sobre el mismo píxel son una).
- [INC-016][INC-021][INC-014] → decision-making.md §19 (mide DÓNDE está el coste; verifica que el flujo se EJECUTÓ sobre el caso real; el test de fixture poblado nunca paga el arranque).
- [INC-017] → decision-making.md §16 +bullet (traducción entre vocabularios con pérdida tiene SIGNO; el badge "traducido" nunca sube de nivel).
- [INC-018][INC-024] → decision-making.md §4 +bullet (tabla de mapeo se testea con el producto cartesiano; fuzzy vs vocabulario real, no vs longitud de una palabra).
- [INC-012][INC-025] → decision-making.md §6 +bullet (veredicto adversarial repetido de clase = ronda; antes del 3er parche cuestiona la FUENTE).
- [INC-011] → truth-loop.md §C (legibilidad ≠ pertenencia). [INC-023][INC-025] → truth-loop.md §A.5 (excepción desperfectos/medidas al mejor-intento).
- Correcciones FACTUALES (no reglas): product.md — árbol Vinted obtenible sin token del SSR; árbol Wallapop endpoint público. Origen: seed fase-4 / [INC-016].
Quedan sin promover (n=1 no irreversible, se mantienen como hechos): la corroboración/cobertura de [INC-012] ya absorbida; INC-004(c) "no generalizar una señal de una clase de producto al catálogo" sigue pendiente y vigente de cara al Frente 2.
═══ fin nota /optimize 2026-07-17 ═══ -->

- [INC-025] 2026-07-17 · proceso · **Tercera ronda sobre `medidas` en una sola sesión → se PARÓ de parchear y se replanteó (§6 aplicada A TIEMPO, por una vez).** Secuencia: (1) `medidas` con "mejor intento" publicaba basura; (2) se movió a un marcador `MEDIDAS:` en el papel; (3) el 1er audit cazó el fuzzy demasiado tolerante (BLOQUEANTE); (4) el 2º audit cazó que `MEDIDA:` singular colisiona a distancia 1 y que una caja imprime sus dimensiones de embalaje bajo "Medidas:" (SERIO). En vez de un TERCER parche sobre el mismo campo, el orquestador reconoció el patrón —`[INC-012]`: "un veredicto que se repite DE CLASE sobre el mismo sitio ES el disparador"— y lo llevó a Diego como decisión de diseño, no de código. **Decisión de Diego: quitar `medidas` del marcador entero.** Motivo estructural, no de implementación: "medida" es una palabra corta y común en packaging (colisión inevitable con cualquier umbral fuzzy útil) y **no existe señal fiable que distinga "dimensión de la caja" de "dimensión del producto"** (mismo límite que `truth-loop.md` §E). Y el coste/beneficio lo remata: el catálogo de Diego es sobre todo ropa, que en Wallapop **ni pide medidas**. `desperfectos` (palabra de 12 letras, sin colisión de packaging a distancia ≤2) se queda con el marcador. · **La lección de método, y es la buena:** a diferencia de `[INC-008]`/`[INC-012]` (donde la §6 se disparó en la ronda 3-4, tarde), aquí se cortó en la ronda 3 **antes de mandar el parche**. La diferencia fue tener el dato del 2º audit (`MEDIDA:` a distancia 1 es estructural, no un bug puntual) y preguntar "¿este campo vale la pena SACARLO de texto?" en vez de "¿cómo arreglo este match?". Simplificar (quitar la fuente) fue más barato y más correcto que el mejor parche posible. · evidencia: 2 `listing-audit` (BLOQUEANTE + SERIO) sobre el marcador; `_distancia_edicion("MEDIDA","MEDIDAS")==1` re-derivado por el orquestador. · estado: pendiente (≥2) — regla candidata: **"antes del 3er parche sobre el mismo campo/sitio, la pregunta no es 'cómo arreglo el match' sino 'vale la pena esta FUENTE de este dato'. Un campo que colisiona por una razón estructural y que la plataforma ni pide se QUITA, no se endurece — simplificar cierra lo que parchear reabre."**

- [INC-024] 2026-07-17 · alucinacion · **Cerré el bug de `desperfectos` por un lado y lo REABRÍ por otro EN EL MISMO COMMIT — con un fuzzy calibrado sobre el eje cómodo y una premisa que escribí sin medir.** El bug original: `desperfectos` leía cualquier texto y lo publicaba como defecto (el NOMBRE impreso del masajeador, un nº de lote manuscrito). Fix: marcador determinista `DESPERFECTOS:`/`MEDIDAS:` que Diego escribe en su nota. Pero el match era fuzzy a distancia ≤3, **calibrado sobre `DESPERFECTOS` (12 letras) y aplicado igual a `MEDIDAS` (7 letras)** — en una palabra de 7, distancia 3 es el 43%. Resultado: `MEDICAL` (dist 2, en el logo de una caja de Diego **ya en caché**) → `medidas="SYSTEM"`; `PERFECTOS` (dist 3) → un desperfecto INVENTADO desde texto de marketing. **El mismo fallo que el fix venía a matar, reencarnado en el mecanismo que lo sustituye.** · **Doble fallo de método, los dos míos:** (1) `decision-making.md` §4 literal —barrí el eje cómodo (¿aguanta ruido de OCR?, sí) y no el incómodo (¿qué palabras REALES entran?, 25 de 30 de packaging); (2) escribí en el comentario del código *"un falso positivo es prácticamente imposible"* — una afirmación de seguridad **sin medir**, que es exactamente la regla de oro del repo (no afirmar sin verificar) violada en un comentario. El auditor la desmintió en un comando, con la caché de Diego. · **La lección: una afirmación de seguridad en un comentario o en un mensaje de commit es una afirmación como cualquier otra — se ejecuta o no se escribe** (misma clase que `[INC-019]`, donde afirmé "MENTIONS_OTHER_BRAND ya no entra" sin ejecutar ese caso; **2ª incidencia**). · evidencia: `listing-audit` (BLOQUEANTE) reproducido y re-derivado por el orquestador: `_distancia_edicion` sobre 30 palabras de packaging → 25 colisiones con `MEDIDAS`; `"YOUR MEDICAL SYSTEM"` de `data/cache/*595bee1.json` → `medidas="SYSTEM"`. · **Fix:** umbral por marcador (MEDIDO contra la lista de palabras reales, no elegido a ojo) + separador `:`/`-` pegado al token + dígito obligatorio en medidas; luego, tras un 2º audit, `medidas` se quitó del marcador entero (`[INC-025]`). · estado: pendiente (≥2 con `[INC-019]`) — regla candidata: **"un umbral fuzzy se calibra y se VERIFICA contra el vocabulario real que puede colisionar, no contra la longitud de UNA de las palabras que separa; y una afirmación de 'esto es imposible/seguro' en un comentario se ejecuta antes de escribirla."**

- [INC-023] 2026-07-17 · alucinacion · **`desperfectos` no leía desperfectos: leía cualquier texto y lo llamaba defecto — y "solo lo manuscrito" NO era el arreglo, porque Diego no es el único que escribe a mano.** Medido sobre 3 recortes reales (el orquestador los miró con sus ojos = ground truth): la nota real de Diego (`CREMALLERA ROTA`, bloc de espiral) salía bien; el **texto IMPRESO** de la caja del masajeador (`masajeador LH; Láser Rodilla`) se publicaba como desperfecto Y con el caption "nota manuscrita"; y un **nº de lote en rotulador azul** sobre una pegatina de la caja (`2250028`) — **manuscrito, pero NO una nota de Diego** — también. El tercer caso mató la hipótesis intuitiva ("filtra lo manuscrito"): está escrito a mano y es basura. · **La causa es la de siempre en este repo:** se le pedía al modelo un JUICIO ("¿qué desperfectos ves?") que no puede dar, en vez de una TRANSCRIPCIÓN ("copia este texto") que hace con 0 alucinaciones. Es `[INC-010]`/`[INC-012]` (el error no es el algoritmo, es QUIÉN DECIDE) y el patrón de la cremallera de `curar.py`: el fallo caro se vuelve **inexpresable**, no "avisado". · **Solución de Diego (2026-07-17): el papel se identifica SOLO, con una palabra clave que él escribe** (`DESPERFECTOS: cremallera rota`). Convierte "juzga si esto es un defecto" (imposible) en "busca el marcador y copia lo de detrás" (`if`, determinista). El sensor es el boli de Diego; el modelo sólo transcribe. **Invierte el default MEJOR INTENTO SÓLO para este campo**, y está justificado con dato: el mejor intento acertó 1/3 y produjo ruido; en un campo sin juicio posible, "mejor intento" no es un intento. `fuente="foto"` sólo si el valor está en el píxel citado; sin marcador → `null` (cae del lado barato, Diego lo teclea). · evidencia: los 3 recortes reales mirados a ojo + reproducción ejecutada de `_extraer_valor_tras_marcador`. · estado: pendiente (≥2 con `[INC-010]`) — regla candidata: **"un campo que exige un JUICIO que el modelo no puede dar (¿es esto un defecto? ¿es de este producto?) no se saca pidiéndoselo mejor: se convierte en una TRANSCRIPCIÓN de una señal que el humano controla (una palabra clave en un papel), o se deja vacío. El sensor barato es la mano del usuario, no un prompt más listo."**

- [INC-021] 2026-07-17 · proceso · **Diego llevaba DOS DÍAS juzgando un diseño que su pantalla nunca había ejecutado — y nadie lo comprobó hasta que insistió por tercera vez.** Reportó *"en ficha sale todo vacío... iría más rápido subiéndolo directamente en wallapop"*. El orquestador **no especuló** (bien) y consultó su store real: de sus 7 productos, **1 extraído con 9 de 11 campos LLENOS** (los 2 vacíos eran `talla`/`medidas` de un masajeador de rodilla: correctos) y **6 con `campos = {}` — la extracción NUNCA se ejecutó sobre ellos**. Causa: extraer era **por producto y a mano**, 2 clics + 2 esperas cada uno = **14 clics** antes de que la máquina rellenara nada. · **El detalle que lo convierte en incidente y no en una feature que faltaba: el único producto extraído era la CAJA** (el masajeador). Sus **6 sudaderas no se habían extraído nunca**, así que **el giro null→mejor-intento del 2026-07-16 — la decisión de producto más importante del proyecto — no se había ejecutado sobre ropa NI UNA VEZ**. Se cerró la Fase 2 declarándola medida, se escribió `truth-loop.md` §A.2 sobre esa premisa, y se construyó la Fase 3 encima; todo sobre **n=1, y el caso cómodo** (una caja con el nombre impreso, que es justo el caso que `[INC-004]` ya avisó que NO se generaliza al catálogo de Diego). · **La métrica primaria (`CLAUDE.md`: "minimizar segundos-hasta-publicar") volvió a no usarse como criterio** — es `[INC-016]` otra vez, en otra escala: allí se optimizó el 5% del tiempo ignorando el 66%; aquí se pulió la CALIDAD de una pantalla que costaba 14 clics arrancar. Ningún test lo vio porque todos los tests arrancan un store sintético ya extraído: **la suite nunca paga el coste de arrancar**. · evidencia: consulta directa a `data/resellermaster.db` (7 productos, 6 con `campos={}`); `ui/ficha.py:563` (botón por producto) → `_dialog_extraer` → 2º botón. · **Fix:** `🔎 Extraer TODO el lote` con UNA puerta de coste para N productos (14 clics → 2); coste real medido de sus 6 sudaderas: **3,8 cts** (41 de 47 llamadas ya cacheadas). · estado: pendiente (≥2, con `[INC-016]`) — regla candidata: **"antes de juzgar la CALIDAD de un output, verifica que el usuario lo ha EJECUTADO alguna vez sobre su caso real. Un flujo que cuesta N clics arrancar no se ha probado hasta que alguien pagó esos N clics — y los tests, que arrancan de un fixture ya poblado, jamás los pagan."**

- [INC-020] 2026-07-17 · alucinacion · **El bug de Diego (`CREMALLERA ROTA` ausente de la descripción) se arregló… y se REINTRODUJO POR OTRA PUERTA en el mismo commit.** El fix regeneraba el título/descripción al confirmar, desde los campos confirmados, con un prompt que **EXIGE** mencionar `desperfectos` si existe. Pero **nadie comprobaba que el modelo obedeciera**: reproducido con `desperfectos="mancha en el logo de Nike"` y una descripción que no lo mencionaba → **se confirmó y persistió sin un solo aviso**. Antes el texto era viejo y omitía el desperfecto; ahora es nuevo y lo omite igual. · **Cuarta incidencia de `[INC-010]`** ("el código hacía `if` sobre la autoevaluación del sospechoso"): el propio módulo tiene escrito *"un prompt es una peticion; un `if` es una garantia"* — y aquí sólo había petición. Es el campo que evita la devolución. · **Segundo hallazgo, y es de diseño de prompt: el prompt se CONTRADECÍA a sí mismo.** Regla 1: *"NO menciones NINGUNA marca distinta de la seleccionada"*. Regla 3: *"DEBES mencionar el desperfecto **con esas mismas palabras**"*. Con `desperfectos="mancha en el logo de Nike"`, **gana la regla 3** y la marca ajena entra por un campo de texto libre que el propio pipeline le sirve. Los campos `desperfectos`/`modelo`/`titulo`/`color` son texto libre y pueden contener una marca: "el input son sólo campos confirmados" no basta como garantía si los campos son prosa. · evidencia: `listing-audit` (2026-07-17), reproducido y re-derivado por el orquestador ejecutando. 584 tests en verde. · **Fix:** verificación DETERMINISTA (solapamiento de palabras significativas + anexar el literal si falta) — **nunca** otra llamada al LLM para verificar, que sería el mismo sospechoso autoevaluándose. · estado: pendiente (≥2 con `[INC-010]/[INC-015]`) — regla candidata: **"cuando un prompt EXIGE algo del output, ese algo se verifica con un `if` determinista sobre el output, o no está exigido: está pedido. Y dos reglas de un prompt pueden contradecirse — si una dice 'no menciones X' y otra dice 'copia literal este campo', el campo gana."**

- [INC-019] 2026-07-17 · alucinacion · **La guarda de marca ajena tenía una puerta abierta, y el diseño empujaba justo hacia ella.** `schema.validar_texto` hacía `if marca_seleccionada:` antes de buscar marcas ajenas → con `marca=None` o `""` **no comprobaba nada**. Y `marca` es opcional **A PROPÓSITO** (`"Sin marca"` es un valor válido en Vinted, `product.md` #4; se excluyó de `_CAMPOS_OBLIGATORIOS` deliberadamente) → **el único caso que la defensa no cubría era exactamente el que el diseño fomenta**. Reproducido: descripción con *"mancha en el logo de Nike"* + `marca=None` → `validar_texto` devuelve `[]` y `construir_payload` **exporta limpio**. Vinted OCULTA el anuncio: silencioso, Diego lo ve publicado y sin ventas. · **La ironía que prueba el bug: `export._campo_marca` sustituye por `"Sin marca"` DESPUÉS de que `validar_texto` corriera con `None`.** Se saneaba contra el hueco y se publicaba `"Sin marca"` — y con `"Sin marca"` el chequeo **sí** salta. La defensa miraba un valor que nunca se publica. · **El agravante de método, y es mío: el mensaje del commit anterior afirmaba que `MENTIONS_OTHER_BRAND` "deja de tener por dónde entrar".** Lo escribí yo, sin ejecutar ese caso. El auditor lo refutó en un comando. **Una afirmación de seguridad en un mensaje de commit es una afirmación como cualquier otra: se ejecuta o no se hace.** · evidencia: `listing-audit` (2026-07-17, BLOQUEANTE) reproducido y re-derivado por el orquestador: `validar_texto(t,'vinted',None) -> []` vs `validar_texto(t,'vinted','Sin marca') -> ['MENTIONS_OTHER_BRAND']`. 584 tests en verde. · **Fix:** en `schema.validar_texto`, el único sitio → los dos call sites quedan arreglados sin tocarlos. · estado: pendiente (≥2) — regla candidata: **"una guarda condicionada a que un campo TENGA valor deja sin defensa exactamente el caso 'sin valor' — y si ese campo es opcional por diseño, ése es el caso MÁS común, no el raro. Sanea contra el valor que SE VA A PUBLICAR (el default incluido), nunca contra el hueco."**

- [INC-018] 2026-07-17 · proceso · **El `listing-audit` del export volvió a auditar el EJE CÓMODO en el test, y el test consagró el bug en vez de cazarlo.** `tests/test_export.py::test_estado_para_reparar_electronica_vinted` barría **electrónica** — la única categoría donde Vinted SÍ tiene el literal `"Necesita reparación"` y el mapeo es fiel — y fijaba **moda**, que es exactamente donde el mapeo pierde gravedad y donde vive el catálogo de Diego. Test verde, `[INC-017]` intacto. · **Es `decision-making.md` §4 literal** ("barre el eje INCÓMODO, no el cómodo"), que es una regla **ya promovida** desde `[INC-002]/[INC-003]/[INC-005]`: escrita, vigente, y violada otra vez por un agente que escribió el código y su test a la vez. **Cuarta incidencia de la clase.** Confirma lo que ya dijo `[INC-010]`: *un test escrito por el mismo que escribió el código puede estar de acuerdo con él en el error* — el agente eligió el caso donde su propia implementación quedaba bien. · evidencia: `tests/test_export.py:216` (verde) vs. la reproducción del orquestador con `categoria="moda"` → estado falso con `traducido=True`. Lo cazó el `listing-audit` adversarial, NO la suite (470 tests en verde con el bug dentro). · estado: pendiente (≥2) — regla candidata: **"cuando una tabla de mapeo tiene un eje categórico (categoría, plataforma, idioma), el test lo BARRE ENTERO — el producto cartesiano, no un representante. Elegir un representante es elegir el cómodo; y quien escribe el test es quien escribió el código."**

- [INC-017] 2026-07-17 · plataforma · **QUINTA vez que la confianza sale ANTI-CORRELACIONADA con el riesgo (`[INC-002]/[INC-005]/[INC-009]/[INC-010]`) — ahora en el EXPORT, y con una prenda ROTA.** `core/export.py` traducía `EstadoCanonico.PARA_REPARAR` en **moda** al literal `"En condiciones aceptables"` (Wallapop) / `"Satisfactorio"` (Vinted) — que significan *usada pero funcional* — y lo marcaba **`traducido=True`**, que es el marcador de máxima confianza del módulo y significa literalmente *"pega esto, no re-decidas"*. Sin `nota`, sin aviso. En paralelo, el campo **`desperfectos`, que Diego escribe A MANO** (`"ROTO: agujero de 5 cm en la manga izquierda"`), **no salía en ningún payload**: es literalmente el campo que evita la devolución, y se perdía por el camino junto con `color`, `medidas` y `modelo`. Resultado reproducido: sudadera rota publicada como funcional, descripción sin mencionar el agujero, cero señales. · **Lo que NO era el bug, y es lo que lo hace sutil: elegir `"Satisfactorio"` es CORRECTO y forzado** — Vinted no tiene `"Necesita reparación"` fuera de electrónica y su regla oficial (help/50) es "ante la duda, el más bajo". `ACEPTABLE` es el suelo disponible. **El bug era el SILENCIO**: el módulo emitía su insignia de "no re-decidas" justo en el único caso donde Diego TIENE que hacer algo más. Es `[INC-005]` con otra ropa: el output más peligroso se llevaba la confianza más alta, que es justo la que se pasa en bloque sin mirar. · **Fix estructural, no caso a caso:** se construye el INVERSO de las tablas de mapeo (literal → el `EstadoCanonico` que ese literal COMUNICA al comprador) y `schema.fidelidad_estado()` compara: si el literal comunica un nivel **mejor** que el real → `traducido=False` + nota + aviso. Presentar el producto **peor** (`PRECINTADO→"Nuevo"`, `MUY_BUENO→"Buen estado"`) es seguro y no se marca — marcar lo seguro convertiría el aviso en ruido y entrenaría a ignorarlo (`decision-making.md` §12). Así, **cualquier edición futura de las tablas que presente el producto mejor de lo que es queda cazada sola**, sin que nadie recuerde esta sesión. · Dos hallazgos SERIOS del mismo audit, también cerrados: (a) **`aviso_coherencia` se perdía entero en el export** — `[INC-011]`, la ficha Frankenstein: si dos prendas se fusionaron, el export publicaba marca de una y talla de otra con `traducido=True` y sin una palabra; el fixture del test hardcodeaba `aviso_coherencia: None`, así que ese camino **no se ejercía nunca**; (b) el filtro de marca ajena se **vendía como bloqueo duro** en el docstring siendo best-effort: medido sobre las marcas REALES del golden set, **`Umbro`/`Lufthous`/`Original Marines`/`New Age` pasan — 4 de 5 — y solo `Nike` lo dispara**. Que sea best-effort no era el bug (`schema.py` lo documenta con honestidad); venderlo como garantía, sí. · evidencia: `listing-audit` (2026-07-17, veredicto BLOQUEANTE), **reproducido y re-derivado por el orquestador ejecutando** `construir_payload` con la ficha rota; 470 tests estaban en VERDE. · estado: pendiente (≥2, con `[INC-005]/[INC-009]/[INC-010]`) — regla candidata: **"toda TRADUCCIÓN entre vocabularios (canónico → literal de plataforma) es potencialmente con pérdida, y la pérdida tiene SIGNO: presentar el producto peor de lo que es es seguro, presentarlo mejor es una mentira. El signo se deriva invirtiendo la tabla, no se recuerda a mano — y el badge de 'ya traducido' NUNCA se emite sobre un mapeo que sube de nivel."**

- [INC-016] 2026-07-16 · proceso · **El orquestador (yo) pasó una sesión entera optimizando el 5% del tiempo de Diego mientras el 66% estaba sin construir — y no lo detectó ninguna regla, lo detectó Diego diciendo "no siento que me ahorre tiempo".** Se debatieron durante horas Google Lens, SerpAPI vs Bright Data, búsqueda por imagen, scrapers open source y visión para el agrupado. Un panel de 4 agentes (2 diseños ciegos + 2 ataques) midió el desglose real de segundos-hasta-publicar por producto: **agrupar+revisar+precio ~90 s (33%) vs EXPORT ~285 s (66%)** — y **el export no existe**. Todo lo debatido tocaba el precio (~20 s/producto). La visión en el agrupado, que se iba a construir, ahorra **~30 s en TODO el lote (0,7%)** y encima **auto-fusionar reintroduce el fallo catastrófico** (hoy la máquina nunca fusiona sola: 0 fusiones medidas). · **La causa no fue falta de rigor** — cada decisión estaba medida y auditada; fue que **nadie midió DÓNDE está el tiempo antes de decidir qué optimizar**. La métrica primaria del proyecto (`CLAUDE.md`: "minimizar segundos-hasta-publicar") **existía y no se usó nunca como criterio de priorización**. · evidencia: panel de 4 agentes (2026-07-16), desglose por etapa con supuestos explícitos; `docs/seeds/fase-3-export.md`. · estado: pendiente (≥2) — regla candidata: **"antes de elegir QUÉ optimizar, medir DÓNDE está el coste (segundos de Diego, no elegancia). La métrica primaria del proyecto es un criterio de priorización, no un lema del README."** Emparenta con `[INC-001]`/`[INC-007]` (enunciar la capacidad mínima antes de elegir la herramienta), pero es una clase distinta: allí se elegía mal la HERRAMIENTA; aquí se eligió mal el PROBLEMA.

- [INC-015] 2026-07-16 · alucinacion · **`fuente="foto"` validaba que la CITA existiera, no que el VALOR estuviera en ese píxel — un `if` que no forzaba lo que su propio docstring prometía.** El paso de síntesis liga el recorte al texto citado por el modelo (`de_texto_detectado`), pero **nunca comprobaba que `valor` coincidiera con ese texto**. Reproducido ejecutando: `valor="Nike Air Classic"` citando el crop de `"Reebok"` → `Campo(fuente="foto", evidencia=<crop de Reebok>)`. La variante peligrosa **no es la contradicción sino la EXTENSIÓN**: `valor="Reebok Classic Leather 100% algodon"` citando `"Reebok"` → el crop dice "Reebok", el campo empieza por "Reebok", **a Diego le cuadra de un vistazo y confirma** — y "Classic Leather 100% algodon", que nunca fue legible, viaja como `fuente=foto`. El prompt pedía "no contradigas lo visible", así que el vector realista era justo ése. · **Misma clase que `[INC-009]`** ("un docstring no es una defensa"): el docstring del módulo prometía *"'foto' SOLO si hay un candidato REAL que lo respalde"* y el código respaldaba la **cita**, no el **valor**. Segunda incidencia de la clase. · evidencia: `listing-audit` (2026-07-16, veredicto BLOQUEANTE) reproducido y re-derivado por el orquestador; fix + test en `tests/test_extract.py::test_sintesis_fuente_foto_exige_que_el_valor_este_en_el_pixel`. · **Fix aplicado:** `fuente="foto"` exige que `valor` esté **contenido en el texto legible del candidato citado**; si no, degrada a `inferido` (el crop pasa a ser contexto, no evidencia). · estado: pendiente (≥2 con `[INC-009]`) — regla candidata: **"cuando un `if` autoriza una garantía, el `if` tiene que comprobar EXACTAMENTE lo que la garantía promete, no un proxy correlacionado (la cita ≠ el valor)."**

- [INC-014] 2026-07-16 · verificacion · **La UI mostró `marca` VACÍA teniendo `"Lufthous"` persistido en disco — el dato era correcto, la pantalla mentía.** Streamlit **ignora el `value=`/`index=` de un widget cuya `key` ya está en `session_state`**; una extracción anterior (de una versión del código donde la marca salía `null`) dejó la key pegada en `""` y tapó el valor nuevo durante toda la sesión de navegador. Diego lo reportó como *"me sigue sin detectar la marca, está casi todo en null, creo que no es normal"* — y tenía razón: **no era normal, pero tampoco era lo que parecía**. · **La lección de método:** el orquestador NO especuló — consultó el store (`marca="Lufthous"` ✅), ejecutó `_valor_por_defecto` sobre el dato real (devolvía `"Lufthous"` ✅) y renderizó la pantalla con `AppTest` en estado limpio (mostraba `"Lufthous"` ✅). Los tres verdes **acorralaron el bug en el único sitio que quedaba**: el `session_state` sucio de la sesión viva, que ningún test veía porque los tests siempre arrancan limpios. · evidencia: `tests/test_ficha.py::test_valor_pegado_de_extraccion_vieja_se_refresca` (siembra la key sucia y comprueba que se refresca). · **Fix:** `_sembrar_valores_iniciales` siembra `session_state` desde el dato y **re-siembra cuando la extracción cambia** (firma distinta) → re-extraer resetea los campos y el valor pegado se limpia solo. · estado: pendiente (≥2) — regla candidata: **"un test que siempre arranca en estado limpio no puede ver un bug de estado sucio acumulado; cuando la app real y el test discrepan, el sospechoso es el estado que el test no reproduce."**

- [INC-013] 2026-07-16 · proceso · **El sanitizador de texto (`schema.validar_texto`/`es_exportable`) llevaba días construido, con tests, y NADIE LO LLAMABA.** `product.md` §7 lo declara *"defensa con dientes: si no pasa, no se exporta"*, y `decision-making.md` §12 exige que toda defensa **bloquee**. `grep` sobre el repo: sólo aparecía en `schema.py` (definición) y `test_schema.py`. La síntesis guardaba `titulo`/`descripcion` verbatim y la UI los pintaba sin validar → una descripción con una marca ajena (que en Vinted **oculta el anuncio**) llegaba a confirmación intacta. · **La clase:** una defensa construida, testeada y **no cableada** es exactamente igual de útil que no tenerla — y encima da una falsa sensación de cobertura ("el sanitizador existe" aparece en la doc y en los tests). Lo cazó un `listing-audit`, no la suite: **421 tests en verde con la defensa desconectada**. · evidencia: `listing-audit` (2026-07-16); `grep -rn "es_exportable\|validar_texto"` → 0 llamadas fuera de tests. · **Fix aplicado:** cableado en la confirmación de ficha (`ui/ficha.py::_problemas_de_texto`), bloqueando por violaciones de CONTENIDO (marca ajena/email/enlace); pendiente cablearlo también en el export (Fase 3). · estado: pendiente (≥2) — regla candidata: **"una defensa no existe hasta que algo la LLAMA: al construir una, el mismo cambio tiene que incluir su punto de llamada y un test que demuestre que BLOQUEA de verdad, no que la función devuelve `False`."**

- [INC-012] 2026-07-14 · alucinacion · **TRES `listing-audit` BLOQUEANTE seguidos sobre `core/extract.py`, con los tests en verde las tres veces (371 → 397 → 401). El approach entero estaba roto, no los bugs — y el segundo arreglo EMPEORÓ la trampa principal del golden set.** La "solución" de `[INC-010]` fue exigir **corroboración independiente** para emitir `confianza=alta` (el OCR como testigo del VLM). El tercer pase la demolió, y tenía razón: **el testigo NO es independiente — el OCR y el VLM leen EL MISMO CROP.** Si el recorte está mal, los dos se equivocan igual y **se corroboran mutuamente** → `alta` sobre un dato falso. Peor: `difflib` mide sustitución de caracteres, que es **exactamente** el modo de fallo del OCR, así que "el OCR se comió una letra" y "el VLM leyó otra marca" son **indistinguibles** para la métrica. Medido: **`XL`/`XXL` corroboran con 0.800** — y las tallas reales de Diego son XXL, XL, XXS, M. **Ninguna ventana de operación existe**: subir el umbral mata los pares reales (`Reebok`/`Raabdk`=0.500) antes que a los falsos amigos (`Nike`/`Mike`=0.750, `Puma`/`Puna`=0.750). La etiqueta de la Jack & Jones lleva `Originals` en cursiva grande (es la LÍNEA, no la marca) y el rediseño la publicaba como `marca='Originals'` con `alta` **y doble corroboración** — la trampa que el gate marca en mayúsculas como FALSA salía con más aplomo que antes del arreglo. · **El dato que cerró el debate no fue una alucinación, fue la COBERTURA: con un VLM ORÁCULO (leyendo perfecto), marca 2/5 prendas y talla 1/5.** Aunque el modelo acierte, el pipeline no sirve — `truth-loop.md` §C ya avisaba de que existe "un umbral de cobertura por debajo del cual la app deja de ahorrar tiempo y pierde su razón de ser", y estábamos debajo. Causa: un crop → un solo campo en el esquema, así que el recorte que contiene `Reebok` **Y** la talla `M` sólo podía rendir uno de los dos. · **LA LECCIÓN, y es la de `[INC-008]` en otro dominio: el error no era el algoritmo, era QUIÉN DECIDE.** Se intentaba que el pipeline **decidiera la verdad solo**. La Fase 1 ya lo había aprendido (la agrupación no la arregló un algoritmo mejor: la arregló la cremallera, que hace el fallo caro *inexpresable* y deja la decisión en Diego), y `core/pricing.py` —la costura más limpia del repo— funciona por lo mismo: **no tasa, enseña dónde mirar.** Decisión de Diego (2026-07-14): **el extractor NO AFIRMA — PROPONE un valor y ENSEÑA EL PÍXEL del que lo sacó; Diego confirma de un vistazo.** `confianza=alta` desaparece salvo para lo verificable por una **regla matemática, no por un modelo** (un EAN cuyo checksum GS1 valida). · **Meta-fallo del orquestador, que es lo que hay que corregir: la regla anti-bucle (`decision-making.md` §6) debió dispararse en la ronda 2, no en la 3.** Cuando el pase 2 devolvió BLOQUEANTE sobre la MISMA clase (confianza alta sobre dato falso), eso ya era la señal de "rediseña, no parchees" — y en vez de eso mandé un tercer parche (el matiz "testigo en contra"). Lo que la paró fue un auditor midiendo la **cobertura**, no la regla. **Un veredicto adversarial que se repite de clase ES el disparador, exactamente igual que 2 rondas sin converger.** · evidencia: 3 veredictos BLOQUEANTE consecutivos (`listing-audit` ×3, ángulos independientes: "el VLM miente" / "el VLM es honesto y la realidad es rara" / "verifica el fix y busca lo que ROMPIÓ"); barrido del umbral de similitud (u=0.4 → 5/5 falsos amigos peligrosos pasan; u=0.8 → sólo 2/6 pares reales sobreviven); cobertura con VLM oráculo sobre el lote real. Re-derivado por el orquestador ejecutando cada caso. · estado: pendiente (≥2) — reglas candidatas: (a) **"dos señales que miran el MISMO píxel no son dos señales: son una. Una corroboración sólo cuenta si su modo de fallo es INDEPENDIENTE del de la señal que corrobora"**; (b) **"antes de blindar la verdad de un output, MIDE SU COBERTURA: un pipeline que no se equivoca nunca porque no afirma nada tampoco sirve, y la cobertura se mide con un ORÁCULO (asumiendo que el modelo acierta), lo que es gratis y no necesita API"**; (c) **"un veredicto adversarial BLOQUEANTE que se repite DE CLASE cuenta como ronda a efectos de la regla anti-bucle: 2 veredictos de la misma clase → rediseñar, no parchear"**.

- [INC-011] 2026-07-14 · alucinacion · **La FICHA FRANKENSTEIN: `core/extract.py` daba por VERDAD la agrupación, cuando `core/grouping.py` entero está construido sobre la premisa de que NO lo es.** Si un grupo trae fotos de dos productos (una fusión que Diego no cazó al curar — el fallo que `truth-loop.md` §E llama "el más caro y el más silencioso"), la marca sale de una prenda y la talla de la otra: `marca='JACK & JONES' (alta, ev=IMG_110552)` + `talla='XXL' (alta, ev=IMG_110449)` → se publica **una Jack & Jones talla XXL**, cuando la J&J es XL y la XXL es de la Reebok gris. · **Lo que lo hace letal, y es el hallazgo de método: el fallo ATRAVIESA LAS TRES CAPAS del `truth-loop`.** Los dos campos tienen evidencia REAL y LEGIBLE, así que la Capa 2 (`listing-audit` preguntando "¿se ve este dato en este píxel?") responde **LIMPIA a los dos**. La única capa que lo caza es el ojo de Diego — que es exactamente para lo que la app existe para no tener que usar. **Un gate de legibilidad no detecta un error de PERTENENCIA:** cada dato es cierto, pero no son del mismo producto. · **Y la defensa estaba anti-correlacionada con el riesgo, otra vez:** el conflicto sólo se dispara si el OCR localiza las etiquetas de AMBAS prendas; en el caso normal (el OCR falla en la mitad de las etiquetas, medido) hay **un solo candidato → `confianza=alta`**. La abstención sólo llegaba cuando había DEMASIADA evidencia. · evidencia: `_construir_campo_texto` con las lecturas reales de P3+P4 fusionados → reproducido por el orquestador ejecutándolo; `listing-audit` pase B (ángulo "el VLM es honesto, la realidad es rara") con los crops reales mirados a ojo. · estado: pendiente (≥2) — regla candidata: **"un gate que verifica LEGIBILIDAD no verifica PERTENENCIA. Todo módulo que agregue datos de VARIAS fotos debe emitir un aviso de coherencia cuando los campos provienen de fotos disjuntas — y ese aviso tiene que tener DIENTES (techo de confianza), no ser un pie de foto (`[INC-008]`)."**

- [INC-010] 2026-07-14 · alucinacion · **TERCERA vez que la confianza sale ANTI-CORRELACIONADA con el riesgo (`[INC-005]`, `[INC-009]`, y ahora ésta) — y la primera en el pipeline de ATRIBUTOS, no en el de agrupación.** `core/extract.py` emitía `confianza="alta"` cuando había **UNA SOLA SEÑAL**: un candidato único → `alta`; tres fotos que coinciden → `alta`. **Coincidir no es acertar: puede ser el mismo sesgo repetido.** Medido sobre las fotos reales: la sudadera **negra** sale `color='gris oscuro'` con `alta` porque la autoexposición del móvil levanta el negro a RGB(80,80,80) **en las tres fotos igual** → varianza cero → confianza máxima. Con **una sola foto**, `alta` trivialmente (`len(normalizados)==1`), violando la regla #9 escrita en el docstring del propio módulo ("nunca de una sola foto: es un sorteo"). · **La causa estructural, que es lo que hay que recordar: todas las reglas duras se aplicaban sobre campos que el propio VLM se AUTODECLARA** (`¿esto es una etiqueta de cuello?`, `¿esto es legible?`, `¿esto pertenece al producto?`). El código hacía `if` sobre la autoevaluación del sospechoso. **Preguntar no es verificar.** Bastaba con que el modelo dijera `ubicacion="etiqueta_interior"` sobre el estampado para colar `marca='ORIGINALS'` con `alta`, o `legible=true` sobre la palabra tapada por un cable para colar `modelo='Pocket Life'`. El docstring prometía "exclusión estructural"; el código pedía obediencia — es `[INC-009]` con otra ropa. · **Segundo hallazgo, y es el que casi se cuela: una defensa se COMIÓ a la otra.** La regla #1 ("el estampado nunca es la marca", nacida de la Jack & Jones) filtraba por ubicación **ANTES** de que la regla #2 ("dos marcas legibles → null + pregunta a Diego", nacida de la UMBRO/RAMI JALAB) pudiera contar los candidatos → el UMBRO bordado se borraba y **el conflicto nunca existía**: `marca='RAMI JALAB' confianza='alta' conflictos=()`. El pipeline ELEGÍA, que es justo lo que la regla prohibía. **El test que "probaba" la regla #2 le daba de comer un input imposible** (etiquetaba el bordado del pecho como `etiqueta_interior`, algo que el propio prompt prohíbe) → test verde, trampa intacta. Un test escrito por el mismo que escribió el código puede estar de acuerdo con él en el error. · **Ley nueva que sale de aquí, y es la de `truth-loop.md` §E aplicada a los atributos:** *el VLM puede PROPONER, pero no puede CONFIRMAR él solo. Ninguna afirmación sale con `alta` sin CORROBORACIÓN INDEPENDIENTE; una sola señal, por nítida que parezca, tiene techo `media`.* La corroboración existía y era **gratis**: el OCR ya leyó algo en esa misma región (por eso hay un crop). `Reebok`/`Raabdk` se parecen → corrobora. `Nike`/`Raabdk` no → el modelo está inventando. · evidencia: DOS `listing-audit` independientes, ambos BLOQUEANTE, convergiendo en la misma causa raíz desde ángulos opuestos (uno asumió que el modelo miente; el otro que el modelo es honesto y la realidad es rara). Re-derivado por el orquestador ejecutando ambos casos. 371 tests estaban en VERDE. · estado: pendiente (≥2)

- [INC-009] 2026-07-14 · agrupacion · **La cremallera (curar.py, 5º intento de la pantalla) degradaba hacia FUSIONAR en el caso de fallo — la misma inversión de `[INC-005]`, en una función que prometía lo contrario en su propio docstring.** El diseño (elegido por un panel adversarial de 21 agentes) tiene un suelo de seguridad fuerte: la única operación de la UI es `toggle(costura)`, así que el espacio de estados alcanzable son SOLO particiones contiguas → meter la foto del producto A en el grupo del B es inexpresable, no sólo desaconsejado. Pero `costuras_abiertas_de` (el inverso que deriva las costuras abiertas del estado persistido) tenía `grupo_de_foto.get(izq) != grupo_de_foto.get(der)`, y con dos fotos HUÉRFANAS (sin producto) eso es `None != None` → `False` → costura CERRADA → fusión. Su docstring prometía "degrada al lado seguro, sobre-cortar"; el código hacía lo contrario. **Alcanzable de verdad:** cuando la propuesta inicial falla al persistir (StoreError transitorio de SQLite bajo WAL con 2 pestañas, o el flag `legible` envenenado por un PermissionError del antivirus de Windows en la ingesta), TODAS las fotos quedan huérfanas → la pantalla presentaba las 33 fotos como UN producto confirmable. Los dos `listing-audit` independientes lo encontraron (uno lo llevó hasta confirmado en SQLite: los 7 productos reales en un grupo). · **La lección, y es la de `[INC-005]` con otro nombre: en el caso de MÁXIMA incertidumbre (ninguna señal), preguntar SIEMPRE hacia dónde degrada el código — y verificar que el default cae del lado barato (sobre-cortar), no del caro (fusionar). "Sin datos" nunca puede resolverse hacia unir.** El docstring decía lo correcto y el código decía lo contrario: un docstring no es una defensa. · evidencia: `costuras_abiertas_de(['a','b','c','d','e'], {})` → `set()` → `particion` → 1 grupo de 5; tras el fix (`gi is None or gd is None or gi != gd`) → 5 singletons; exploit del pase A re-ejecutado contra el fix → no reproduce; 264 tests + verificación LIMPIA del tercer pase sobre las 33 fotos reales. · estado: **promovido → `decision-making.md` §16** (2026-07-14, con `[INC-005]`).

- [INC-008] 2026-07-14 · verificacion · **El layout de 2 columnas (idea de Diego para ir rápido) llevó 4 rondas de `listing-audit` BLOQUEANTE seguidas, todas con los tests en verde, hasta que un panel adversarial de 21 agentes demostró que el problema NO eran los bugs: era el LAYOUT.** Cada ronda arreglaba los hallazgos y la siguiente encontraba otros nuevos, todos de la misma clase: un botón global ("Añadir aquí") que movía fotos que Diego no veía → mezclar dos productos lejanos en un click. Se parcheó mostrando los nombres de fichero; veredicto: "no es una defensa, es un pie de foto". **La regla anti-bucle (`decision-making.md` §6: 2 rondas sin converger → PARA y replantea el approach, no el parámetro) debió dispararse en la ronda 2, no en la 4.** Lo que la disparó al final no fue el orquestador: fue Diego preguntando "¿hay alternativas mejores?". El panel (5 diseños ciegos entre sí + 15 jueces adversariales) convergió solo en un modelo de datos distinto — la FRONTERA como unidad, no la foto — que hace el fallo caro inexpresable por construcción. También mató 3 anti-patrones que parecían buenas ideas: pintar el hueco temporal como ayuda (está ANTI-correlacionado con la respuesta: los huecos a UNIR son 71/94 s, el que hay que CORTAR es 23 s), el contador "→ 7 productos" como validación (checksum con colisiones), y auto-fusionar con el clasificador de pago (la máquina ejecutaría la operación cara y la "defendería" con un badge). · evidencia: 4 veredictos BLOQUEANTE consecutivos sobre `ui/confirmacion.py`; workflow `panel-ui-curado` (21 agentes, ranking + síntesis); la cremallera resultante pasó doble `listing-audit` + verificación (LIMPIA) con 264 tests. · estado: **promovido → `decision-making.md` §6 (rediseñar, no parchear) y §12 (mostrar ≠ defender)** (2026-07-14).

- [INC-007] 2026-07-14 · proceso · **Tercera vez que Diego caza el MISMO patrón con la MISMA pregunta, y la tercera vez que tenía razón.** Le presenté el clasificador de tipo de foto como "necesita un modelo de visión de pago" (encuadre: *visión* → VLM → API). Él preguntó: *"¿no se puede usar mi suscripción? ¿y GLM u otro modelo chino barato?"* — y esa pregunta me hizo ver que había saltado a "visión de pago" **sin probar la señal de TEXTO**, cuando las tres cosas que hay que detectar (etiqueta, papel escrito, metro) son literalmente texto. Es el encuadre de `[INC-001]` clavado, y la regla candidata (b) de `[INC-004]` dice textualmente *"ante 'agrupar/identificar productos', probar la señal de TEXTO (OCR) antes que la visual/semántica"*. **La escribí yo y no la apliqué.** Registrada 3 veces, aplicada 0. · **Desenlace, y esto es lo importante: la hipótesis del OCR se MIDIÓ y salió FALSA** — así que la conclusión (hace falta visión) era correcta, pero por suerte, no por método. Medido con RapidOCR sobre las 33 fotos reales: los caracteres detectados en las fotos que ABREN un producto real `[5, 9, 9, 20, 28, 39, 72]` y en las que abren un corte de más `[0, 18, 32, 33, 36, 176]` **se solapan por completo**. Razón: los productos 1 y 2 son CAJAS (su plano general está lleno de texto impreso) y las sudaderas casi no tienen texto (un plano general con `Reebok` = 9 chars es indistinguible de una foto de detalle = 0 chars). El discriminante real es la COMPOSICIÓN de la imagen (¿se ve la prenda entera o un trozo?), que es genuinamente visual. Coste de refutarla: **5 minutos**. Coste de haberla construido sin medir: otra ronda de `[INC-002]`. · **Hallazgos colaterales, gratis y valiosos:** (1) el metro se delata solo — el OCR lee `69899995999291909698925959556575`, una ristra de dígitos: regla trivial y gratuita que caza 1 de los 6 cortes de más; (2) **el OCR lee las marcas y tallas sin pagar nada** (`Reebok`, `XXL`, `UMBRO`, `ORIGINAL MARINES`, `New Age`, `RAMI SALAB`, `XXS`) → buena parte de la Fase 2 (atributos) puede salir gratis; (3) **verificado en documentación oficial: la suscripción Pro/Max de Claude NO da acceso programático** y Anthropic prohíbe explícitamente enrutar peticiones de apps de terceros por credenciales de plan de consumidor. Hace falta API key de Console, facturada aparte. Haiku 4.5 con visión: ~1.600 tokens/imagen a $1/MTok ≈ **0,2 cts/foto ≈ 1 ct/producto**, y con caché por hash se paga una sola vez. · estado: **promovido → `decision-making.md` §7 (capacidad mínima antes de una dependencia de pago) y §2 ("¿seguro?" → medir, no argumentar)** (2026-07-14, con `[INC-001]` y `[INC-004]`).

- [INC-006] 2026-07-14 · agrupacion · **La UI de confirmación petaba al mover una foto, y el bug HERMANO —el silencioso— dejaba fotos pre-marcadas en el grupo destino sin que Diego las marcara.** Lo cazó Diego probando la app, con **245 tests en verde**. (1) El crash: `_limpiar_seleccion` escribía `st.session_state["sel_{foto_id}"] = False` desde el cuerpo del script, después de que el checkbox con esa misma key ya se hubiera instanciado en el mismo rerun → `StreamlitAPIException`, siempre, en cada "Mover". **Sin pérdida de datos** (verificado releyendo el store de disco: `guardar_agrupacion` commitea en SQLite `BEGIN IMMEDIATE`/WAL *antes* de la excepción) — pero la UI **miente sobre lo que pasó**: Diego ve un traceback rojo y el movimiento SÍ se aplicó. Cree que falló, y funcionó. (2) El bug hermano, que es el peligroso: el botón **"Fusionar"** movía todas las fotos del grupo y **nunca limpiaba la selección** → **no petaba**, y las fotos aterrizaban en el grupo destino **con el checkbox ya marcado**. Un click de más ahí = una foto del producto A en la ficha del B (`truth-loop.md` §E: el fallo más caro y el más silencioso). El camino que crasheaba era el ruidoso; el que callaba era el que podía contaminar una ficha. · **Por qué nadie lo vio: `tests/` no tenía NI UN test de `ui/`.** Los 241 tests verdes (schema, images, store, grouping, golden set) no tocaban una sola línea de esa pantalla. `streamlit.testing.v1.AppTest` funciona perfectamente aquí y reproduce el bug en 10 líneas — no era caro, era que no estaba. Instancia literal de "verde local ≠ ficha correcta" (`change-loop.md` §C4). · evidencia: traceback de Diego (`ui/confirmacion.py:187`); `bug-hunter` re-derivado por el orquestador; los 4 tests nuevos de `tests/test_ui_confirmacion.py` verificados **rojos contra el código viejo y verdes contra el nuevo** (`git stash` mediante) — el de Fusionar falla en rojo **sin excepción**, sólo con el checkbox marcado: exactamente la firma del fallo silencioso. · **Fix:** los dos botones pasan a `on_click` callbacks (corren en `on_script_will_rerun`, antes de instanciar widgets → escribir keys `sel_*` ahí es legal por construcción, no por suerte de orden), y **ambos** limpian la selección. Marcados con comentario los sitios de `app.py` donde el mismo patrón es legal **sólo por el ORDEN** (`sb_pantalla`, `sb_lote_id`): es una condición de corrección, no un detalle de estilo. · estado: **promovido → `decision-making.md` §11 (el gemelo silencioso) y `change-loop.md` §C4 (tests de `AppTest` en la UI de curado)** (2026-07-14).

- [INC-005] 2026-07-14 · agrupacion · **La confianza estaba ANTI-CORRELACIONADA con el riesgo: `core/grouping.py` v4 firmaba las fusiones con `confianza=alta`, que es justo la que la UI confirma en bloque sin mirar.** v4 fue la primera versión medida sobre las fotos reales de Diego (33 fotos, 7 productos) y pasó el gate del golden set (6/6 fronteras, 0 fusiones, 5 cortes de más). Aun así, **dos `listing-audit` independientes la declararon BLOQUEANTE**, y ambos tenían razón — reproducido y re-derivado por el orquestador ejecutando: (1) v4 concedía `alta` cuando el hueco interno máximo del grupo era pequeño; pero **una fusión la CAUSA un hueco pequeño** (Diego cambia de producto rápido) → el grupo fusionado no tiene ninguna pausa interna y se lleva la confianza MÁS ALTA. Medido: cambio de producto en 9 s → `productos=[A,B] conf=ALTA n=6`. Cuanto más peligroso el lote, más confiado el output. (2) Con **EXIF degenerado** (12 fotos, 2 productos, timestamps idénticos) devolvía **un solo grupo con los 2 productos, `conf=alta`**, motivo "12 fotos seguidas en 0 s, sin ninguna pausa dentro". Un hueco de 0 s no significa "mismo producto", significa "no sé". (3) El **gate que se escribió para proteger el módulo lo bloqueaba**: `assert cortes_de_mas <= 5` se pone en ROJO al bajar el umbral — que es la reparación que el propio módulo ordena ante una fusión. Una defensa que muerde a quien la obedece, y que empuja a *subir* el umbral, el único movimiento que produce el error caro. (4) Los números que justificaban el umbral de 20 s eran **falsos**: `truth.json` y `truth-loop.md` §E decían "huecos intra 4-8 s / inter 14-36 s"; el EXIF real dice **intra 1-94 s / inter 23-2735 s**. El barrido completo (5→30 s), que nadie había hecho, muestra zona segura 1-23 s y acantilado en 24 s: **20 s dejaba 4 s de colchón, menos de dos jitters (±2.4 s)**. · **Lección que NO es "los tests estaban mal":** el golden set no cazó nada de esto porque sólo probaba **un lote y la dirección cómoda** (subir el umbral fusiona). El eje donde vivía el fallo era el contrario — un cambio de producto **más rápido** que el umbral — y no estaba testeado. Es literalmente la regla candidata (c) de `[INC-003]` ("una simulación que barre un eje y fija otro no valida nada"), registrada y **no aplicada**. Tercera incidencia de esa clase. · evidencia: `pytest tests/test_grouping_golden.py -s` (barrido de umbrales + `test_cambio_rapido_de_producto_*` + `test_exif_degenerado_*`, todos nuevos); reproducción directa de las dos fusiones con `patch.object(grouping, "leer_metadatos", ...)`. · **Fix aplicado (v5):** `alta` no se emite nunca mientras el reloj sea la única señal (el reloj puede PARTIR, no puede CONFIRMAR); EXIF degenerado → cajón de INCIERTAS; ficheros ilegibles → grupo propio con el error real (v4 les decía "agrúpala a mano" a ficheros corruptos); umbral 20 → **15** (colchón 4 s → 9 s, coste: 1 corte de más); el gate ya sólo asierta `FUSIONES == 0` y **reporta** los cortes de más en vez de asertarlos. · estado: **promovido → `decision-making.md` §16 (caso de fallo, confianza y gates) y §4 (barrer el eje incómodo)** (2026-07-14, con `[INC-009]`, `[INC-002]`, `[INC-003]`).

- [INC-004] 2026-07-14 · verificacion · **Toda la agrupación se construyó sobre dos señales que las fotos reales de Diego NO tienen o donde FALLAN — y nadie miró una foto suya hasta la ronda 4.** Medido con sus 13 fotos reales (2 productos): (1) **WhatsApp borra el EXIF entero** — 13/13 fotos con **cero tags**, así que la señal primaria (timestamp) sobre la que se hicieron 3 rondas de arreglo **no existe** en su flujo real; (2) **CLIP falla en fotos reales**: partió el producto A en dos y **metió 3 fotos de A en el grupo de B** — porque no mira el producto, mira el sofá, el portátil y la caja blanca, idénticos en las 13; (3) **el OCR acertó 11/13 con CERO contaminación y 2 abstenciones** (justo las 2 fotos sin texto). Es la **misma clase de error que `[INC-001]`**: alcanzar la señal visual/semántica cuando la señal era **texto**. Segunda incidencia independiente de la misma clase. · evidencia: `getexif()` → 0 tags en las 13; matriz de similitud CLIP (rango 0.55-0.88, sin separación bimodal) y 2-means → grupo contaminado; OCR (RapidOCR) → `LUFTHOUS/LaserRodilla` vs `NEWAGE/Pocket`, 0 errores. También en la caja: **código EAN 8445061029720** = identificador global y único del producto, que da marca, modelo y consulta de precio sin ninguna similitud visual. · **CAVEAT que NO se debe olvidar:** esos 2 productos eran **de caja con el nombre impreso**. Diego confirma que **la mayoría de su catálogo es SIN caja** (ropa y variado). **El resultado del OCR NO se transfiere a ropa** — concluir que sí sería repetir el error de generalizar desde la muestra cómoda. Hace falta un lote real de ropa, por cable, antes de rediseñar. · estado: **promovido → `decision-making.md` §7** (2026-07-14, con `[INC-001]` y `[INC-007]`: la candidata (a) y la (b), fusionadas en "enuncia la capacidad mínima y MIDE la alternativa gratuita sobre datos reales de Diego"). **La (c) sigue `pendiente` y es la que más vale de cara a la Fase 2**: "una señal medida sobre UNA clase de producto no se generaliza al catálogo entero sin medirla en las demás" — el OCR acertó 11/13 en **cajas con el nombre impreso**, y la mayoría del catálogo de Diego es **ropa sin caja**. n=1, no irreversible → no toca reglas todavía, pero es la trampa exacta en la que puede caer la extracción de atributos.

- [INC-003] 2026-07-13 · proceso · **Tres rondas de arreglo sobre `core/grouping.py` sin converger, todas calibradas contra imágenes sintéticas.** El fallo no se corrigió: **cambió de signo**. v1 elegía la frontera de *mayor* salto → un hueco anómalamente ALTO (una pausa larga) empujaba el umbral arriba → **fusionaba** 8 prendas en un grupo. v2 elige la frontera *más baja* que califica → un hueco anómalamente BAJO (un disparo 2 s más rápido) empuja el umbral abajo → **tritura** 3 prendas en 8 grupos; end-to-end, 6 prendas → 16 grupos, **0 correctos**. Mismo defecto estructural las dos veces: **un umbral global derivado de un estadístico de un EXTREMO de la distribución de huecos.** Y cada vez, los tests pasaban en verde (11 → 22 → 27): barrían el eje `ratio inter/intra` y **nunca el jitter**, que es el eje donde vive el fallo — la simulación que "validaba" la constante fijó el jitter en ±1 s, justo el último valor donde el algoritmo aún funciona. · evidencia: `_umbral_corte([4,4,12,4,4,12,4,4]) = 8.0` → 3 grupos (correcto); `_umbral_corte([2,4,12,4,4,12,4,4]) = 3.0` → 8 grupos (mismas 3 prendas). Ventana de operación medida: funciona hasta jitter ±1.5 s, 5.67 productos partidos/lote a ±2.5 s. · **PARADO por la regla anti-bucle (`decision-making.md` §6): los datos de entrada (fixtures sintéticos) no son fiables → se deja de simular y se pide el dato real.** El parámetro que decide la viabilidad del módulo **no es ninguno de los que se llevan tres rondas calibrando**: es el **jitter real del ritmo de fotografiado de Diego**, que se mide GRATIS leyendo el EXIF de una carpeta suya cualquiera — sin CLIP, sin golden set, sin anotar nada. · estado: pendiente (≥2) — reglas candidatas: (a) "un umbral de separación NUNCA se deriva de un extremo de la distribución (ni el máximo salto ni el mínimo que califica): usar separación bimodal / clustering"; (b) "**antes de calibrar un parámetro contra datos sintéticos, preguntar qué dato real lo fijaría y cuánto cuesta conseguirlo** — aquí costaba 10 minutos y se gastaron tres rondas de ingeniería"; (c) "una simulación que barre un eje y fija otro no valida nada: hay que barrer el eje donde puede vivir el fallo, no el cómodo".

- [INC-002] 2026-07-13 · agrupacion · `core/grouping.py` metía **8 prendas distintas en UN grupo con `confianza="alta"`** con el ritmo de fotografiado real de un lote (3 fotos/prenda cada 4 s, 12 s para cambiar de prenda): el umbral de corte `mediana×3` da exactamente 12, y `12 > 12` es falso → cero cortes. El confirmador por pHash que debía cazarlo es **ciego al color** (phash promedia a luminancia): camiseta roja vs azul = distancia **0**, indistinguibles. Los dos fallos se refuerzan y el output sale marcado `alta`, la vía por la que la UI está diseñada para que Diego NO mire. · evidencia: `listing-audit` (barrido de 8 ritmos + medición de distancias pHash), re-derivado por el orquestador ejecutando el caso: `mediana=4 umbral=12 cortes=0`; `phash(roja) - phash(azul) = 0`. **Los 11 tests de `test_grouping.py` pasaban en verde** — sólo cubrían ratio inter/intra 7,5× cuando el fallo vive por debajo de 3,5×. · estado: pendiente (≥2) — reglas candidatas: (a) "un umbral derivado de la MEDIANA de una distribución no separa outliers: usar p75+1.5·IQR o separación bimodal"; (b) "si la señal primaria no pudo aplicarse (sin umbral derivable), la confianza NUNCA puede ser `alta` — el techo es `media`"; (c) "pHash es luminancia: no es una señal válida de identidad de producto cuando el color es el discriminante".

- [INC-002] … (ver arriba) · estado: **promovido → `decision-making.md` §4** (2026-07-14, con `[INC-003]`, `[INC-005]` — barrer el eje incómodo). Las reglas candidatas (a) y (c) del umbral y el pHash **NO se promueven**: eran específicas del diseño de `grouping` v1-v4, que ya no existe (v5 no deriva ningún umbral de la distribución ni usa pHash). Muertas por obsolescencia, no por convergencia.

- [INC-001] 2026-07-13 · proceso · Propuse un plan con dos APIs de pago (LLM de visión + SerpAPI) **sin haber explorado en serio la alternativa gratuita**. Encuadré el problema como "visión" (→ VLM, → 4 GB VRAM insuficientes, → API de pago) cuando el problema real era **leer texto en una etiqueta** (→ OCR local en CPU, gratis). Lo cazó Diego preguntando "¿seguro no hay mejor opción?". Peor aún: el stack gratis (OCR) está **más alineado con el `truth-loop` que el de pago** — un OCR no puede alucinar una marca, un VLM sí — o sea que escribí una regla que argumentaba contra mi propia recomendación y no lo vi. · evidencia: `rules/truth-loop.md` §A ("ningún atributo sin que sea legible en una foto") vs. el plan propuesto en la misma sesión. · estado: **promovido → `decision-making.md` §7** (2026-07-14, con `[INC-004]` y `[INC-007]`).

<!-- nota /optimize 2026-07-14: esta entrada [INC-001] estaba DUPLICADA literalmente (misma fecha, mismo texto). Eliminada la copia: un duplicado exacto no es historia, es un defecto de append — y en un ledger que cuenta incidencias para decidir promociones, contar dos veces la misma habría bastado para promover una regla desde n=1. -->

- [INC-003] … (ver arriba) · estado: **promovido → `decision-making.md` §4** (2026-07-14, la candidata (c): barrer el eje incómodo). La (b) ("¿qué dato real fijaría este parámetro y cuánto cuesta conseguirlo?") queda **absorbida por §7** — es la misma pregunta que la de la dependencia de pago, aplicada a un parámetro.
