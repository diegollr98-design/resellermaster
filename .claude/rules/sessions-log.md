# Sessions Log — RESELLERMASTER

Bitácora por hito. **Más reciente arriba.** Mantener ≤ 100 líneas: las entradas viejas se archivan en `docs/sessions-log-archive.md` (fuera del contexto) vía `/optimize` Paso 5c.

**Plantilla de entrada:**
```
## vX.Y — fecha — <título> <✅|🔴>
**Qué se hizo:** ...
**Incidentes:** [id] del ledger, si los hubo
**Verificación:** qué se ejecutó y qué demostró (salida real, no "reportó que OK")
**Pendiente:** ...
```

---

## v0.12 — 2026-08-05 — CIERRE por auditoría: gates presenciales MEDIDOS + bug real `[INC-031]` ✅
**Qué se hizo:** Sesión de cierre (seed `docs/seeds/cierre-ultra-review.md`). **`/seed-review` → ✏️ CON EDICIONES:** el panel (agente ciego + 2 críticos) convergió en que el SEED cometía un PROXY —auditar código con 899 tests verdes mientras la métrica-norte "segundos-hasta-publicar" NUNCA se había medido tras 5 fases (reincidencia de `[INC-016]`/§19)— y en que los gates presenciales estaban mal degradados a "cabos". Reorden aceptado por Diego: **medir primero**. **GATE A MEDIDO: export real de 1 producto en ~210 s con la app vs ~285 s baseline a mano → la app SÍ ahorra tiempo** (el número que faltaba desde la Fase 1, la métrica primaria del proyecto por fin instrumentada). **GATE B: venta real end-to-end** (Subido→Vendido→Excel) + **prueba anti-corrupción PASADA** — cambiar el `coste_cents` tras vender NO altera el beneficio histórico (lee de `coste_snap_cents`, snapshot inmutable; editar coste no requiere re-extraer ni re-confirmar, es `on_change` directo a columna propia).
**Incidentes:** **`[INC-031]`** (`5aa8a99`): la síntesis fallaba con **400 de esquema en TODA re-extracción** —`genero` con `type:["string","null"]`+`enum`, que structured outputs de Anthropic rechaza— cazado por Diego en el Gate A, no por la suite (los tests mockean el motor y descartan el `json_schema`, nunca lo mandan a la API real). Misma clase que `[INC-021]`/`[INC-028]` (verde local ≠ funciona). Fix: `anyOf`; guard determinista red→green. El banner verde misatribuía la causa ("saturación temporal"), clase `[INC-030]`.
**Verificación:** `bug-hunter` re-derivado por el orquestador (evidencia en `extract.py:1784`/`llm.py:647`); doc oficial de structured outputs confirma `anyOf` soportado y `type`-array+`enum` no; **900 passed, 1 skipped**; ruff limpio; **fix verificado END-TO-END por Diego re-extrayendo** (síntesis completa); ambos gates ejecutados por Diego en vivo.
**Auditoría acotada HECHA (`[INC-032]`, commit `a77c45c`):** 3 `bug-hunter` en paralelo sobre la superficie NO presenciada (store v3/Finanzas), validados por ejecución contra el `.db` REAL. Superficie mayormente SÓLIDA (guarda de dinero/FK/atomicidad de `borrar_lote`, migración additiva, snapshot inmutable, sanitizador con dientes, ref en ambas plataformas, traducción-con-signo cartesiano 70 filas — todo resistió). 3 hallazgos reales arreglados: reporte coste↔beneficio incoherente (coste vivo vs snapshot), reset del contador que ignoraba `publicaciones` (comentario con afirmación de seguridad falsa, `§17`), aviso al vender con coste 0. 904 passed, 1 skipped.
**Pendiente:** (1) **PORTFOLIO HTML** (Diego aporta el `portfolio-fable-ultra` de referencia) — con la historia MEDIDA (210 vs 285 s) y la orquestación multi-agente del propio cierre. (2) `/optimize` cuando toque (`[INC-031]`/`[INC-032]` esperan ≥2). Precio en Vinted (Datadome) sigue como cabo.

## v0.11 — 2026-07-21/23 — Fase 5 FINANZAS implementada + `[INC-030b]` (registro reconciliado por `/optimize` 2026-08-04) ✅
**Qué se hizo:** *(esta entrada la escribe `/optimize` reconciliando el git log + la suite; la sesión de Fase 5 no dejó bitácora propia.)* **`core/store.py` v3** (commits `6bb68de`…`81b1e61`): cierra los 3 riesgos de corrupción del seed de forma ESTRUCTURAL — `referencia`/`coste_cents` como columnas propias (fuera del JSON `campos` que `guardar_extraccion` sobreescribe), contador de referencia como marca de agua persistente (`referencia_seq` AUTOINCREMENT, nunca `MAX()+1`), `ventas` como tabla aparte con snapshot inmutable que sobrevive al borrado; migración additiva v2→v3 sin DROP. **UI Fase 5** (`6a898c1`): coste + nº de ref en Ficha (ref idempotente), botón "Subido" por plataforma en Export (imprime la ref en la descripción), marcar "Vendido" (precio auto desde Export + editable + plataforma) → beneficio, pantalla "5. Finanzas" con export `.xlsx`. **`borrar_lote`** (`27299ca`): guarda de dinero (bloquea si hay ventas) + borrado transaccional hijos→padres + `AppTest` que PULSA el botón (`[INC-028]`).
**Incidentes:** `[INC-030b]` (`32f35a3`, pre-fix `04ce0ab`): confirmar el producto A disparaba `st.rerun()` a mitad del bucle → GC de las keys de widget de los productos posteriores sin confirmar; la re-siembra por firma lo trataba como re-extracción y revertía al disco, pisando la elección de Diego que sólo vivía en `session_state` (grave en `estado`). Fix: sombra `_shadow_{key}` que sobrevive al GC. **Disco SIEMPRE intacto** (store append-only). Misma clase que `[INC-030]`, ya regla (§19) — añadido al ledger como HECHO de refuerzo, no promueve nada nuevo.
**Verificación:** `/optimize` corrió la suite completa hoy (2026-08-04): **899 passed, 1 skipped** (el skip es el test VLM-real opt-in, no un fallo). No re-derivé los veredictos de la sesión de Fase 5 (no la presencié); el modelo de datos anti-corrupción se lee en `store.py` v3 y coincide con los 3 riesgos del seed.
**Pendiente:** **EL GATE REAL** (Diego cronometra un export, baseline ~285 s — la métrica primaria, sin medir tras 5 fases) + **gate presencial de la Fase 5** (registrar una venta real). Precio en Vinted (Datadome). *(El `test_curar` flaky quedó resuelto: `timeout=30` en `AppTest` — era el warmup de Streamlit bajo carga, no un bug.)*

## v0.10 — 2026-07-21 — `tipo` + `género` en la extracción, título que lidera con el tipo, 2 bugs de Diego + `/optimize` ✅
**Qué se hizo:** Sesión larga (planificación→implementación→uso en vivo). (1) **`tipo` de producto** (qué ES: "masajeador de rodilla", "sudadera") — texto corto libre, `fuente=inferido` SIEMPRE, en la síntesis (0 llamadas extra, espejo de `categoria`/`estado`). Consumidores: **título** (prompt v2 LIDERA con el tipo + backstop determinista `_asegurar_titulo_lidera_con_tipo`, dedup por núcleo, tope 100), **precio** (`_tipo_para_busqueda` prefiere el `tipo` confirmado sobre `_TIPOS_PRENDA`; `[INC-027]` intacta), **candidatas de categoría** (`texto_busqueda`). Forma decidida CON Diego (texto libre, no enum: un enum tira el tipo que no está — el masajeador). (2) **`género`** (fast-follow) — enum cerrado hombre/mujer/niño/niña/unisex, nullable, inferido; alimenta las candidatas (`_factor_genero`). (3) **Botón "Re-confirmar seleccionados"** (regenera títulos en bloque sin re-extraer; reusa `_accion_confirmar_lote`, 0 lógica nueva). (4) **`/optimize`.**
**Incidentes:** `[INC-029]` (la síntesis, la llamada VLM 21 y más valiosa, se caía por un 429 tras la ráfaga de crops → `max_retries=2` del SDK se agotaba; fix `max_retries=8` EN LA COSTURA + UI honesta ante `vlm_sintesis:` + motivo veraz). `[INC-030]` (la ficha se vaciaba al cambiar de tab / desde un 2º localhost: Streamlit GC de las keys de WIDGET, el marcador de firma sobrevive → siembra saltada; fix re-sembrar por key). **Ambos con el disco SIEMPRE intacto** — Diego temió 2× perder datos y no perdió nada → promovidos JUNTOS a `decision-making §19` ("ante un reporte de datos perdidos, verifica el disco ANTES; store append-only; vacío-en-pantalla es un problema de LECTURA"). El fix del 429 lo verificó un panel adversarial (2 diseños ciegos + 1 crítico) que afinó la propuesta del orquestador (mató "correr síntesis antes"—imposible—y "bloquear export"—redundante).
**Verificación:** **813 passed, 1 skipped** (el flaky `test_curar` de siempre); ruff limpio; app usada EN VIVO por Diego (título lidera con el tipo, re-confirmar en bloque, ficha ya no se vacía). Auditado con DATOS REALES: constructores de tipo/género hardcodean `inferido` (sin rama a foto/alta); género sesga candidatas reales (hombre≠mujer en el árbol); backstop de título sobre los 7 títulos reales; 2 tests red→green del GC. `listing-audit` sobre tipo (RIESGO/0-BLOQUEANTE) y sobre título (RIESGO; fix #5 dedup por núcleo aplicado). Género con gate proporcional (espejo de `categoria` ya auditado, inferido-only, bajo riesgo).
**Pendiente:** **EL GATE REAL** (Diego cronometra un export, baseline ~285 s) — la métrica que dice si esto ahorra tiempo, sin hacer varias fases. Precio en Vinted (Datadome); `género`→título/descripción si lo quiere; endurecer el `test_curar` flaky.

## v0.9 — 2026-07-21 — Pulido del EXPORT con Diego usándolo en vivo + `/optimize` ✅
**Qué se hizo:** Sesión larga puliendo el export mientras Diego lo probaba con sus fotos reales. (1) **Precio**: palabras clave EDITABLES (una combinación por línea, sembradas con sugerencias generosas que incluyen la marca aunque sea `inferido` — Diego edita y ve los comparables, así que la procedencia estricta se relaja SÓLO en la vía editable); `tasar_variantes`/`tasar_terminos`; AUTO-BUSCAR al abrir «4. Export» (menos clics); URL → `st.link_button`. (2) **Orden del formulario POR PLATAFORMA** (`_ORDEN_BLOQUES` + dispatcher): Wallapop (Resumen→Fotos→Categoría→Título→Descripción→Marca/Talla→Estado→Precio→Medidas→Tamaño) y Vinted (Fotos→Título→Descripción→Categoría→Marca→Talla→Medidas→Estado→Color→Material→Precio→Envío), verbatim de Diego. (3) **Color** añadido al export (Vinted lo pide); **labels de `package_size` de Vinted** VERIFICADOS por Diego (Pequeño/Mediano/Grande). (4) **`/optimize`**: promovidos `[INC-026]`→§4, `[INC-027]`→truth-loop §D.2, `[INC-028]`→change-loop §C4.
**Incidentes:** `[INC-026]` (stems de género escritos a ojo → "hombre" singular no casaba, feature de sesgo invertida; cazado por listing-audit). `[INC-027]` (mediana de un identificativo DÉBIL = catálogo entero disfrazado de precio; ahora exige marca/modelo). `[INC-028]` (rename por `sed` dejó una referencia rota `pricing._NOTA_PRECIO_PEDIDO` que sólo cazó Diego pulsando el botón — el arranque no paga el clic). Los dos primeros los cazó el `listing-audit` (RIESGO, sin BLOQUEANTE); el precio v2 pasó auditoría antes de cerrar.
**Verificación:** ~734 passed (1 flaky pre-existente: `test_curar::test_render_sin_excepcion`, timeout de AppTest a 3s bajo carga — pasa 5/5 aislado, no lo toca ningún cambio de esta sesión); ruff limpio; app arranca sin traceback; precio v2 verificado END-TO-END contra el endpoint real de Wallapop; todos los hallazgos de audit re-derivados ejecutando.
**Pendiente (SESIÓN FRESCA):** **`tipo` de producto en la extracción** — hueco que detectó Diego: la síntesis NO produce qué ES el producto ("masajeador de rodilla", "sudadera", "jersey"). Es fundamental para el TÍTULO, el PRECIO (palabra clave) y las CANDIDATAS de categoría. Seed: `docs/seeds/fase-4-tipo-producto.md`. Otros: el GATE real (cronometrar un export), precio en Vinted (Datadome), endurecer el `AppTest` flaky.

## v0.8 — 2026-07-18 — Fase 4: candidatas de CATEGORÍA (Frente 1) + PRECIO por comparables (Frente 2) ✅
**Qué se hizo:** (1) **Frente 1 — categoría (el mayor ahorro), 0 €, sin tocar `extract.py`:** `core/categorias.py` + `core/taxonomia/{wallapop,vinted}.json` (859/2482 hojas, árboles PÚBLICOS bajados y versionados; refresco `python -m core.categorias --refrescar`). Dado (categoría amplia, título+desc) propone 2-3 HOJAS candidatas rankeadas (IDF + factor de género/infantil), Diego elige con 1 clic; **NUNCA auto-elige** (§18: una hoja mal = anuncio oculto). Envío sugerido sesgado hacia arriba (Wallapop enum verificado; Vinted sin inventar literal); ubicación ya no avisa (Wallapop la autocompleta). (2) **Frente 2 — precio v2 `tasar()`, 0 €:** lee la búsqueda PÚBLICA de Wallapop por TEXTO (marca+tipo+talla, nunca EAN) vía un `Buscador` inyectable (`BuscadorWallapop`, GET plano, sin stealth, solo lectura — Diego aprobó el approach), devuelve **mediana + rango + las URLs** de ~15 parecidos. Sin `n≥5` → `precio=None` + motivo (conjunto inmutable). Etiquetado SIEMPRE "mediana de parecidos", nunca "el precio de tu producto". `atributos_desde_campos` deriva `tipo` del título confirmado. UI bajo demanda con la nota honesta "precio PEDIDO, no de venta".
**Incidentes:** `[INC-026]` (Frente 1: los stems de género escritos a ojo → "hombre" singular no casaba y salía todo Mujer; derivar con `_stem`. + snapshot ausente tumbaba el export → try/except). `[INC-027]` (Frente 2: la mediana salía de un identificativo DÉBIL —tipo genérico solo— = catálogo entero disfrazado; ahora exige marca/modelo. + `_parsear` no cumplía su contrato "parseo raro → []"; + `bool` colaba como precio 1€). Los dos frentes pasaron `listing-audit` (veredicto RIESGO, sin BLOQUEANTE; el invariante caro resiste). 
**Verificación:** ruff limpio; **717 passed, 1 skipped**; categorías re-derivadas sobre la ropa real de Diego (hoja correcta en top-3, género correcto); `tasar` verificado END-TO-END contra el endpoint REAL de Wallapop (15 comparables de "Reebok sudadera XXL", mediana 20€, URLs reales). Los 3 hallazgos de cada audit re-derivados por el orquestador ejecutando, no por informe.
**Pendiente:** (1) **EL GATE REAL (seed):** Diego exporta un producto de verdad, lo pega en Wallapop y CRONOMETRA (baseline ~285 s) — sin ese número no sabemos qué ahorra. (2) Precio en **Vinted** (su búsqueda está tras Datadome; empezamos solo por Wallapop). (3) Wallapop ropa infantil: candidatas flojas (su árbol es por EDAD, no por prenda) — límite documentado, minoría. (4) Correr `/optimize` cuando toque (`[INC-026]`/`[INC-027]` esperan ≥2).

## v0.7 — 2026-07-17 — `/optimize`: 13 incidentes de deuda promovidos + reglas alineadas con las decisiones vigentes ✅
**Qué se hizo:** Corrido `/optimize` sobre el ledger, que arrastraba **`[INC-013]`…`[INC-025]`** sin promover + 3 viejos (`[INC-010/011/012]`). Consolidados en **pocas reglas afiladas** (no 13 sueltas) para no inflar el contexto que se auto-carga: **3 nuevas** en `decision-making.md` — §17 (una garantía en prosa no lo es hasta que un `if` la fuerza sobre el valor que se publica: `[INC-013/015/019/020/024]`), §18 (quién decide, no el algoritmo — juicio imposible→transcripción o hueco; cobertura con oráculo; dos señales sobre el mismo píxel son una: `[INC-023/010/011/012]`), §19 (mide DÓNDE está el coste; verifica que se EJECUTÓ sobre el caso real: `[INC-016/021/014]`) — y **3 extensiones** — §16 (traducción con pérdida tiene SIGNO: `[INC-017]`), §4 (producto cartesiano de una tabla + fuzzy vs vocabulario real: `[INC-018/024]`), §6 (veredicto repetido de clase = ronda; antes del 3er parche cuestiona la FUENTE: `[INC-012/025]`). En `truth-loop.md`: §C legibilidad≠pertenencia (`[INC-011]`), §A.5 excepción desperfectos/medidas al mejor-intento (`[INC-023/025]`). **Correcciones factuales** en `product.md`: el árbol de categorías de Vinted es obtenible SIN token del SSR (sólo el `id` numérico lo exige); el de Wallapop es un endpoint público. **CLAUDE.md** a v0.7: quitado el andamiaje "las reglas NO están actualizadas" (ya lo están), Fase 3 marcada CERRADA, siguiente paso Fase 4.
**Incidentes:** ninguno nuevo; es la sesión que los PROMUEVE. Nota consolidada de promociones en el ledger (writer único).
**Verificación:** `/optimize` no toca código de producto → sin tests que correr; verificado que las ediciones no tocan el CONJUNTO INMUTABLE (§"LO QUE NUNCA DEBES HACER", superficies sensibles, reglas de oro). `.last-optimize` → 2026-07-17 (resetea el reloj del detector). Reconciliación previa contra el repo: árbol limpio en `c1d0c73`, coincide con el seed.
**Pendiente:** **Fase 4** (seed `fase-4-precio-y-pulido-export.md`): Frente 1 (fricción del export, barato) → el GATE real (Diego cronometra un export de verdad) → Frente 2 (precio v2, sólo tras confirmar el approach de scraping con Diego vía Loop de Cambios).

## v0.6 — 2026-07-16 — Haiku MEDIDO (0 alucinación) + pantalla de ficha + EL GIRO: null→mejor intento 🔴 (falta el EXPORT, que es el 66%)
**Qué se hizo:** (1) **La medición que faltaba, con la API key real**: Haiku 4.5 sobre las 33 fotos → **0 alucinaciones**, se abstiene en las 6 trampas `PRESENTE_ILEGIBLE` (incluido el "Pocket Life" tapado por el cable), lee bien (XXL/M/Reebok/lufthous), **14,5 cts el lote**. Con la síntesis: **23,6 cts / 3,4 cts-producto**, 0 al reprocesar. (2) `ui/ficha.py` (NUEVA): cada campo junto a su recorte, badges 📷 leído / 🧠 inferido, conflictos con sus candidatas, confirmar → `fuente=diego` persistido; `serializar/deserializar_extraccion` + `store.guardar_extraccion`/`confirmar_ficha` (append-only, SIN migración). (3) **EL GIRO (decisión de Diego)**: el default se invierte **null → MEJOR INTENTO** — la síntesis (1 llamada: 3 fotos generales + los textos detectados) **commitea** un valor en cada campo, elige la marca ignorando el fondo (`Lufthous`, no el `Intel Core` del portátil) y redacta título+descripción. Motivo: él revisa cada campo con el píxel delante, así que un hueco le cuesta teclearlo y un valor mal 2 s. **La procedencia NO se tocó**: `fuente=foto` exige que el valor esté contenido en el recorte citado. (4) **Panel de 4 agentes** (2 diseños ciegos + 2 ataques) sobre el replan.
**Incidentes:** el `listing-audit` de la síntesis dio **BLOQUEANTE** real: `fuente="foto"` solo validaba que la CITA existiera, no que el VALOR estuviera en ese píxel → el modelo podía **extender** una lectura ("Reebok" → "Reebok Classic 100% algodón") y colarla como leída. Corregido con guard + test. Otros 2 serios cerrados: sanitizador `validar_texto` existía y **nadie lo llamaba**; la UI no mostraba `fuente`. Un 4º audit marcó el giro como "violación grave" citando las reglas viejas: **falso positivo por docs desactualizados** (ver seed fase-3).
**Verificación:** ruff limpio; **423 passed** (suite sin golden); `test_ficha.py` **19/19** (AppTest, INC-006); oráculo golden **8 passed** (sin regresión); la síntesis corrida **de verdad** sobre el `lufthous` → `marca=Lufthous` eligiendo entre 9 candidatos. Bug de UI cazado ejecutando, no razonando: Streamlit ignora `value=` si la key ya está en `session_state` → un valor de una extracción vieja tapaba el nuevo (`marca` salía vacía teniendo el dato); resuelto con siembra por firma + test que lo reproduce. **Entorno:** AVG hace **MITM de TLS** sobre `api.anthropic.com` y tumbaba las 63 llamadas con "Connection error" — no era la key ni el código (memoria del proyecto).
**El panel (2026-07-16), lo que reencuadra el proyecto:** **el EXPORT es el ~66% del tiempo de Diego (~285 s/producto vs ~90 s todo lo demás) y NO EXISTE.** 3 de 4 agentes. Los 4 mataron la **búsqueda por imagen** (todas las APIs de Lens exigen URL pública; la caja ya se identifica por OCR gratis; la ropa genérica no tiene identidad única). La **visión en el agrupado** se aparca: ahorra **~30 s en TODO el lote** y auto-fusionar **reintroduce el fallo catastrófico**. La **composición** no se busca fuera: en moda de Wallapop el campo **no existe**, en Vinted es opcional, y el sensor barato es la mano de Diego. **Título/descripción no pueden ser prosa libre del LLM** (puede colar una marca ajena → Vinted oculta el anuncio): se componen desde los campos confirmados.
**Pendiente (SESIÓN FRESCA, `docs/seeds/fase-3-export.md`):** (1) **EL EXPORT** — tablas de mapeo (categoría/talla/estado por plataforma) + `core/export.py` + `ui/export.py` + fotos por plataforma. Coste 0 €, es el 66%. (2) Precio: mediana de comparables **por nombre, no por EAN** (Diego verificó que nadie lo pone). (3) **`/optimize` URGENTE**: las reglas contradicen el giro de hoy (`truth-loop` §A.2/§A.3 y §D.2, `architecture.md` sobre scraping) — mientras no se escriban, **cada auditoría futura peleará contra el diseño vigente**.

## v0.5 — 2026-07-15 — Fase 2: las 3 costuras construidas y auditadas; el extractor PROPONE, no afirma 🔴 (falta medir con key + UI)
**Qué se hizo:** Las 3 costuras nuevas: `core/llm.py` (Costura 1: único punto de llamada, caché por hash imagen+modelo+prompt, coste en USD, `estimar_coste_lote` sin key ni red), `core/pricing.py` (Costura 2 v1, 0 €: construye la consulta, `precio=None` inexpresable de violar, atajo EAN con checksum GS1), `core/extract.py` (OCR localiza → recorte a resolución nativa → VLM lee; Haiku reescala a 1568px, la foto entera reproduce el cuello de botella y se paga). Gate nuevo `tests/golden/legibilidad.json` (qué es legible en qué píxel, 7×8, verificado a ojo; las 3 trampas del lote). **Decisión medida antes de gastar un euro:** el OCR local lee marca 3/7 y talla 2/5; un humano 7/7 y 5/5; subir la resolución de entrada del OCR NO cierra la brecha (barrido 960→3072, medido) → es tipografía, no tamaño → el VLM está justificado, y sólo para ese hueco. Composición = 0/33 fotos (la etiqueta no está fotografiada) → `material=null` por diseño, decisión de Diego.
**Incidentes:** INC-010 (confianza `alta` con UNA sola señal; la sudadera negra sale "gris oscuro" alta porque las 3 fotos se equivocan igual — coincidir no es acertar; **4ª vez** de la confianza anti-correlacionada con el riesgo: INC-002/005/009/010). INC-011 (la FICHA FRANKENSTEIN: una fusión no cazada → marca de una prenda + talla de otra, ambas con evidencia real → atraviesa las 3 capas del truth-loop; un gate de LEGIBILIDAD no verifica PERTENENCIA). INC-012 (**3 `listing-audit` BLOQUEANTE seguidos**: la "corroboración OCR↔VLM" no era independiente —mismo crop, mismo fallo, se dan la razón—; `XL`/`XXL` corroboran a 0.80; cobertura con VLM oráculo marca 2/5 talla 1/5 → inútil aunque acierte. Causa de ENFOQUE, no de código: se intentaba que el pipeline DECIDIERA la verdad. Es `[INC-008]` en otro dominio).
**El giro:** Diego decidió que el extractor **NO AFIRMA — PROPONE y ENSEÑA EL PÍXEL** del que sacó cada dato; él confirma de un vistazo. Es el patrón de la cremallera (Fase 1) y del precio (no tasa, enseña comparables): la máquina propone, el humano cierra, el fallo caro se vuelve inexpresable. `confianza=alta` desaparece salvo EAN con checksum. Un crop rinde varios campos (antes 1 crop→1 campo: la talla `M` del crop Reebok-rosa era inalcanzable — ése era el techo real de cobertura). Cobertura nueva (VLM oráculo): prendas marca 5/5, talla 5/5 confirmable; P4/P5 salen como conflicto con ambas candidatas + recorte, y eso ES cobertura.
**Verificación:** 406 tests verdes, ruff limpio, todo re-derivado por el orquestador EJECUTANDO cada caso (no por informe): el conflicto UMBRO/RAMI JALAB → null + baja + ambas; `Nike`/`Raabdk` (testigo en contra) → null; EAN con 1 dígito mal → checksum lo rechaza; la ficha Frankenstein reproducida con las fotos reales; la tabla de cobertura 7×4 leída con mis ojos. **4 auditorías adversariales** sobre `atributos`. Coste MEDIDO: 19,2 cts el lote / 2,75 cts/producto / 0 al reprocesar (corregido el "≈1 ct" FALSO de CLAUDE.md, con tabla y fuente).
**Meta-fallo del orquestador (registrado en INC-012):** la regla anti-bucle debió dispararse en la ronda 2 (2 veredictos BLOQUEANTE de la MISMA clase), no en la 3 — mandé un tercer parche donde tocaba rediseñar. Lo paró un auditor midiendo la COBERTURA, no la regla.
**Pendiente (SESIÓN FRESCA, `docs/seeds/fase-2-continuacion.md`):** (1) **medir con la API key real de Diego** —lo único que falta y lo que decide si el VLM vale: con qué frecuencia miente Haiku en SUS crops; ningún test verde lo sustituye—; (2) `ui/ficha.py` (no existe): pintar cada campo JUNTO A SU RECORTE, aquí vive todo el valor; (3) reescribir `/eval` a cobertura + alucinación adversarial; (4) enchufar el coste estimado a la UI antes de procesar un lote (§15 sin cablear); (5) correr `/optimize`.

## v0.4 — 2026-07-14 — Fase 1 CERRADA: agrupación + curado, lote real confirmado ✅
**Qué se hizo:** `core/grouping.py` reescrito de cero (v5, "el reloj puede PARTIR pero no CONFIRMAR" — cero fusiones sobre las 33 fotos reales, umbral 15 s). La pantalla de curado pasó por 5 rondas de `listing-audit`: el layout de 2 columnas de Diego cazó 4 BLOQUEANTE seguidos (el fallo estaba en el DISEÑO, no en el código, `[INC-008]`), un panel adversarial de 21 agentes eligió **la CREMALLERA CON PESTILLO** (la frontera como unidad; fusionar dos productos lejanos es INEXPRESABLE, no sólo avisado). Features de curado: dividir grupo, unir vecinos, eliminar foto (mover a `descartadas/`, recuperable). **Diego curó y confirmó su primer lote real.**
**Incidentes:** INC-005 (confianza anti-correlacionada con el riesgo), INC-006 (el bug que crashea es el ruidoso; el gemelo silencioso es el peligroso), INC-007 (OCR medido y descartado como clasificador de tipo de foto; 3ª vez del encuadre visión-vs-texto), INC-008 (4 rondas de parcheo = rediseñar), INC-009 (degradación hacia fusionar en el caso de fallo).
**Verificación:** 280 tests verdes, ruff limpio, la app arranca sin traceback. El gate (cerrar 6 costuras → 7 productos) verde sobre las fotos reales. Doble `listing-audit` + verificación en cada superficie sensible. Golden set recuperado tras un borrado accidental de `fotos/` (copias en `data/lotes/`).
**Pendiente:** **Fase 2** (extracción de atributos + precio) en SESIÓN FRESCA con `docs/seeds/fase-2.md`. Decisión clave de Diego: NO da ground truth de atributos → el `/eval` pasa a medir alucinación adversarialmente (legibilidad en el píxel) + su ojo en la revisión. Correr `/optimize` (varios incidentes ya ganaron su regla). Falta preguntar a Diego los segundos-hasta-publicar reales del lote curado.

## v0.1 — 2026-07-13 — Infra agéntica instalada ✅
**Qué se hizo:** repo `git init` en `D:\Resellermaster`. Se replicó el harness de Diego (extraído de ecxm-ops, pumpfun-bot y SEKURA) y se adaptó al modo de fallo de este proyecto: `CLAUDE.md` (portada + índice), `.claude/settings.json` (permisos), `incident-ledger.md` (activa el detector global `optimize-nudge.sh` por opt-in), 4 agentes (`engineer`, `bug-hunter`, `listing-audit`, `flow-qa`), 3 skills (`/optimize`, `/eval`, `/run`), 7 ficheros de reglas. Pieza nueva respecto a los otros repos: **`truth-loop.md`** — el loop que impide que el pipeline invente atributos o precios, con el golden set como gate con dientes.
**Incidentes:** ninguno.
**Verificación:** hardware medido (`RTX 3050 Laptop, 4 GB VRAM, 16 GB RAM, i5-11400H`) → **visión local descartada con dato**, no por intuición. `git status` limpio tras el commit inicial.
**Pendiente:** sesión de planificación del producto (2ª tanda de preguntas a Diego). Sin decidir: proveedor de visión (se decide con `/eval`, no antes), estrategia de precio por comparables, taxonomía de campos Wallapop/Vinted (`rules/product.md` está vacío a propósito). **Cero código de producto todavía** — así lo pidió Diego.
