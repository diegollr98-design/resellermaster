# Fase 1 — cero dependencias de red POR PRODUCTO, cero APIs de pago.
streamlit>=1.40
Pillow>=11.0
pillow-heif>=0.20          # fotos .heic del iPhone
ImageHash>=4.3             # hash perceptual (deteccion de duplicados en core/images.py)

# Confirmacion visual de agrupacion (core/grouping.py, [INC-002] v2):
# embeddings CLIP locales en vez de pHash/colorhash. CPU, sin CUDA — instalar
# SIEMPRE con el indice CPU-only de PyTorch para no tirar del build con CUDA
# (varios GB mas pesado e innecesario, esta app no usa GPU en ningun sitio):
#
#   pip install --index-url https://download.pytorch.org/whl/cpu open-clip-torch numpy
#
# La PRIMERA ejecucion en una maquina nueva descarga los pesos del modelo
# (~350-600 MB segun el formato que cachee huggingface_hub) desde HuggingFace
# Hub a ~/.cache/huggingface/hub/ — hace falta red esa vez. Las ejecuciones
# siguientes (mismo proceso o procesos nuevos, misma maquina) son 100%
# offline, reutilizan esa cache. Cero llamadas de red por producto, cero
# coste. Ver docstring de core/grouping.py para la calibracion y la
# degradacion honesta si el modelo no llega a cargar.
open-clip-torch>=3.0
torch>=2.0                 # CPU-only, ver nota de instalacion arriba
numpy>=1.24

# Fase 2 — COSTURA 1 (core/llm.py): unico SDK de proveedor permitido en todo
# el repo, y solo detras de esa costura. Haiku 4.5 con vision (~0,2 cts/foto,
# cacheado por hash) — ver docstring de core/llm.py y architecture.md.
anthropic>=0.40

# Fase 2 — core/extract.py: "el OCR LOCALIZA. El VLM LEE." OCR local, CPU,
# gratis, cero llamadas a proveedor. Instancia perezosa (solo se carga la
# primera vez que hace falta localizar texto de verdad) — un test que
# mockea el OCR no descarga ni carga nada.
rapidocr-onnxruntime>=1.2

# Dev
pytest>=8.0
ruff>=0.8
